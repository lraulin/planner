interface:
  display_name: "Fix Bug"
  short_description: "Root-cause a bug before fixing it"
  default_prompt: "Use $fix-bug to investigate this bug before fixing it."

policy:
  allow_implicit_invocation: true
