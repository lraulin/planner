.DS_Store

# Local, per-machine Claude Code overrides (shared config lives in .claude/settings.json)
.claude/settings.local.json

# dependencies
/node_modules
/.pnp
.pnp.*

# testing
/coverage

# Screenshots and scratch output from the run-planner driver.
/.artifacts

# Achieve Planner reference captures. Kept on disk as the source material for the
# reimplementation, but out of git — they are large, numerous, and never built against.
/screenshots
agent-os/specs/**/visuals/*
!agent-os/specs/**/visuals/approved-wireframe.md

# next.js
/.next/
/out/

# production
/build

# misc
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files — .env.example is committed as the template
.env*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
todo.tmp.md

# Vendor Neon agent skills (`npx skills add neondatabase/agent-skills`).
# Production Postgres is Neon, but these fire on ordinary "database" / "postgres" /
# "migrations" work, push Neon Auth and neon.ts, and fight Docker-local + self-run
# Better Auth. Pooled vs direct is already in agent-os/standards/database/migrations.md.
.agents/skills/neon/
.agents/skills/neon-postgres/
.agents/skills/neon-postgres-egress-optimizer/
skills-lock.json
