# Local development (matches docker-compose.yml)
DATABASE_URL="postgresql://planner:planner@localhost:5432/planner"

# Production uses a Neon connection string, set in the Vercel dashboard:
# DATABASE_URL="postgresql://USER:PASSWORD@HOST.neon.tech/planner?sslmode=require"
