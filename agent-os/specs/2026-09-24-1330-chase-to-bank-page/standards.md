# Standards for Chase to the bank page

Applied as of standards commit `1808a81d`. References, not copies — see AGENTS.md.

- `agent-os/standards/development/clean-code.md` — fix the cause: the cutover's link removal was
  the source of the unmatched account, not the warning that reported it.
- `agent-os/standards/development/testing.md` — the cutover integration test now asserts the
  link survives.
- `agent-os/standards/development/commits.md` — the fix's commit body records the root cause.

## Deviations

None.
