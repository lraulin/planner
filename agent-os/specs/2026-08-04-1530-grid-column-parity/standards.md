# Standards applied

**Status: frozen / complete** (2026-08-04)

- `agent-os/standards/development/testing.md` — pure tree derivations and formatting get
  focused unit tests; no component tests.
- `agent-os/standards/components/data-grid.md` — a defined column is available to Show
  Fields and to filter/search even when hidden; defaults are per-view presentation.
- `agent-os/standards/development/dates.md` — dates are rendered through `toDateKey`, not
  UTC string slicing.
