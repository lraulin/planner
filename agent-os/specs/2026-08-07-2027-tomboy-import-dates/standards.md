# Standards — Tomboy import creation dates

**Status: frozen / complete** (2026-08-07)

- `agent-os/standards/development/dates.md` — Note Date is a calendar day encoded with
  `fromDateKey` and asserted with `toDateKey`; Tomboy metadata timestamps remain instants.
- `agent-os/standards/development/testing.md` — parsing stays pure and adjacent to unit
  tests; persistence and re-import behavior run against real Postgres.
- `agent-os/standards/development/commits.md` — this delta is one independently revertible
  behavior change linked from its commit.
