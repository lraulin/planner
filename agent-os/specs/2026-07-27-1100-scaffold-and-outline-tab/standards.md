# Standards for Scaffold + Outline Tab

**No standards were applied to this spec.**

`agent-os/standards/index.yml` exists but is empty — it contains only the header comment,
with no standards indexed.

This is expected: the codebase was empty when this spec was shaped, so there were no
established patterns to extract standards from.

## Follow-up

Once this spec lands, the codebase will have real conventions worth capturing — Drizzle
schema style, Server Action patterns, tree-mutation signatures, component structure. Run:

```
/agent-os:discover-standards
/agent-os:index-standards
```

before shaping the next spec, so subsequent work inherits these decisions instead of
re-litigating them.
