# Overnight run — 2026-08-04

Started 22:14. Focus: the Postponed / Deferred Date link, then self-directed.

## The Postponed question, answered

Short version: **the plan to link the two fields is fine — it was just implemented in most
places rather than all of them.** No reconsideration needed in the morning.

The frozen `2026-08-01-2145-deferred-date-model` spec already decided exactly what you
described: `postponed` is the state, `deferred_date` is its expiry, and **expiry is derived
at read time, never swept** — no midnight job, no clock in the database. A routine you tick
off today gets `postponed` + tomorrow's date, and tomorrow it simply reads Not started
again with nothing having written to the row.

That was true in `sliceTree` (the Show Postponed toggle), in the Chooser, and in the Status
column. It was **not** true in the State column — which is the one place you actually look,
and the one your filters run on. `stateColumn` / `abbrStateColumn` read `row.node.state`
raw, so the morning after, the cell still said Postponed, and a view with Postponed unticked
hid the cat litter for good. Grouping by State had the same stale reading.

Fixed rather than redesigned. `ownEffectiveState` collapses an expired own shelf, and the
State column, its abbreviated twin, and the State grouping dimension all use it.

## Shipped

- `3bc7d85` — **Let a shelf that ran out stop saying Postponed.** State column, abbreviated
  State column and group-by-State now read the row's own effective state, so an expired
  deferred date stops reading Postponed and a State filter that excludes Postponed shows the
  routine again the next day. Goals' Status column was a hand-rolled duplicate of the same
  column and now borrows the shared one. Verified by `ownEffectiveState` (5 cases) in
  `src/lib/tree/shelving.test.ts` and "groups an expired shelf under Not started, not
  Postponed" in `src/lib/tree/slice.test.ts` — the latter confirmed failing against the old
  `node.state` reading before the fix went in.

- `4c00057` — **Stop a routine coming back off the shelf reading as underway.**
  `scheduleStatus` derived the shelf correctly and then read the **raw** state for every
  band below it. Since expiry is never swept, a row that came back this morning still
  stores `postponed`, which is not `not_started`, so `started` was true and the entire
  not-started half of the §3.8 ladder was unreachable. Cat litter due again today read
  **Ongoing** — "you're already on this" — instead of **Need to Start**; three days past its
  planned day it read Ongoing instead of Behind Schedule; with no dates at all, Ongoing
  instead of Not Scheduled. Verified by three new cases in `src/lib/tree/status.test.ts`
  (all three failed with `'ongoing'` before the fix) and in the app —
  `.artifacts/planner-shots/tasks-status.png`.

## Verified in the app

`.artifacts/planner-shots/tasks-all.png` — Tasks tab, Next actions off, with two rows set
up by hand in the local DB:

| Row               | Stored      | Deferred            | State column | Status column   |
| ----------------- | ----------- | ------------------- | ------------ | --------------- |
| Regenerating item | `postponed` | 2026-08-02 (past)   | **NS**       | Behind Schedule |
| Recurring task    | `postponed` | 2026-08-06 (future) | **P**        | Deferred        |

Before the first commit both read **P**. Both dev rows were **restored to their original
values** afterwards (`completed`, original deadline), and the Next actions switch was put
back on.

## Tried and backed out

(nothing — neither cycle went sideways)

## Decisions — done

All items from this run were closed on **2026-08-07** (`6fcdb08` and the leave-as-shipped
calls below). Nothing here is still open for a future overnight pass.

- **[done] State column: own shelf vs inherited.** Left as shipped: column uses
  `ownEffectiveState` so the editable cell says what writing to it would change. Inherited
  shelving stays visible in Status ("Deferred") and in hide/filter behaviour. Same call for
  the detail form (`TaskForm`): it shows **stored** state beside Deferred until, not the
  derived grid reading.

- **[done] State column sort order.** Switched to Achieve workflow order via `STATE_ORDER` /
  `stateRank` in the State and Abbreviated State columns, and group-by-State in `slice.ts`.

## Gate status at end of run

Run ended 07:25 on 2026-08-05 — you woke up, so this stopped at the end of the second
cycle rather than at a session limit.

| Gate                       | Result                                                                              |
| -------------------------- | ----------------------------------------------------------------------------------- |
| `npm run typecheck`        | pass                                                                                |
| `npm run lint`             | pass, 0 warnings                                                                    |
| `npm run test:unit`        | pass — 91 files, 1232 tests                                                         |
| `npm run test:integration` | **pass and actually ran** — 107 files, 1634 tests with Postgres up; no skip warning |
| `npm run build`            | pass                                                                                |

Working tree clean, `master` pushed. Local dev database restored to how it was.
