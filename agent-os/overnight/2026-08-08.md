# Overnight run — 2026-08-08

Started 21:37. Focus: self-directed.

Baseline gate at start: typecheck ✅ / lint ✅ / unit ✅ (123 files, 1616 tests) /
integration ✅ (23 files, 483 tests — Postgres up, nothing skipped) / build ✅ / smoke ✅
(23/23 routes).

## Shipped

- `057e9bb` — **Metrics UI styled with a token that does not exist.** `MetricDrawer` and
  `GoalMetricsPanel` used `text-danger`, `bg-danger/10` and `border-danger/40`, but no
  `--color-danger` is defined in `globals.css`, so Tailwind emitted nothing: the drawer's
  Delete button read as an ordinary button, the error banner had no border or fill, and
  error text sat in body ink beside a `role="alert"` promising otherwise. Swapped to
  `priority-a`, the hue the other sixteen destructive surfaces already use. Flagged as out
  of scope by the mobile-swipe spec; this is that follow-up. — verified in the app,
  `.artifacts/planner-shots/metric-drawer.png` (Delete now renders red)

- `ad6c7b8` — **Six `"use server"` exports with no caller anywhere.**
  `listSessionsAction` / `getSessionDetailAction` / `loadExerciseHistoryAction` were
  orphaned when fitness moved to path-based routes (the pages now call the queries
  directly as server components); `renameExerciseAction` and `updateExercisePrefsAction`
  were transitional wrappers, the latter already tagged `@deprecated`;
  `deleteWeeklyPlanAction` was never wired to a control. Each was still a callable POST
  endpoint in the action manifest, so this removes reachable surface, not just lines. —
  verified by `npm run build` + full gate

- `ab6da70` — **Four fitness mutations left with no caller** once those actions went:
  `updateExercisePrefs`, `renameExercise`, `updateExerciseNotes` (one-line forwards to
  `updateExercise` from before it took a partial patch) and `exerciseHasHistory` (a
  duplicate of the guard query `deleteExercise` already runs inline). The integration
  suite's second-user check reached `updateExercise` _through_ `renameExercise`; it now
  calls `updateExercise` directly so the scoping assertion survives the deletion. —
  verified by `npm run test:integration` (23 files, 483 tests, DB up)

## Tried and backed out

_(nothing yet)_

## Needs a decision

_(nothing yet)_

## Noticed but didn't touch

- `src/lib/chooser/dates.ts:40` — `effectiveDeadline` walks parents with no cycle guard
  and no depth cap. So do the ancestor walks in `tree/slice.ts:350` and `:397`. Nothing in
  the tree mutations appears to be able to create a cycle, so this is latent rather than
  live; a shared guarded walk would close it if a bad import ever could.
- `src/lib/priority/letterRank.ts` — 284 lines of ranking logic with no `letterRank.test.ts`
  beside it. It is reached by three bindings whose tests total 43 cases, so it is not
  uncovered, but the bare-letter path (outline only — Chooser and Day always carry a rank)
  is the thin part.
- `src/lib/planning/mutations.ts:124` — `deleteWeeklyPlan` now has only its integration
  test as a caller. Kept: it is a real domain operation with a second-user check, unlike
  the endpoint that wrapped it.

## Gate status at end of run

_(in progress)_
