# Overnight run — 2026-09-05

Started 21:30. Focus: self-directed.

## Shipped

- `7e16b7e` — **A bill charge landing exactly between two occurrences was credited to the
  later one.** `nearestOccurrence` (`src/lib/finances/billSchedule.ts:170`) documents "ties go
  to the earlier occurrence, which is the one already owed" but compared both directions with
  a strict `<`, so an exact tie stayed wherever `Math.round(monthsOut / cadenceMonths)` landed
  — forward. A monthly bill due the 15th charged on 2026-03-01 is 14 days from February's
  occurrence and 14 from March's (February being 28 days), and was credited to March;
  `commitments.ts:166` then reads `nextOccurrenceAfter` of that match as the next expected
  charge, so a whole cycle was skipped and February read as never paid. Backward now wins an
  equal gap. — verified by `billSchedule.test.ts` "breaks an exact tie toward the occurrence
  already owed" and "…when a lead shifts the postings" (both fail before the fix), with the
  24-posting real-rent-history regression still green.

  Worth knowing: the pre-existing test named "breaks a tie toward the occurrence already owed"
  compared 15 days against 16, so nothing exercised a tie at all.

- `8019ce2` — **A parent cycle crashed every grid with a Status column.**
  `scheduleStatusById` (`src/lib/tree/status.ts:263`) is the one downward walk in the tree
  code with no cycle insurance: `walkUp` covers every upward walk and `derive`'s rollup is an
  iterative reverse pass that skips a child it has not reached, both written for a cycle a
  corrupt import can create and `moveNode` refuses to. So `derive` hands the nodes over
  happily and `visit` recursed — it memoizes into `result` only _after_ the child loop, so
  re-entering an open node found nothing cached and A→B→A ran until the stack gave out. A
  `RangeError` out of a server component takes down Outline, Projects, Tasks, Goals and the
  Chooser. Open nodes are now tracked during the walk. — verified by `status.test.ts`
  "survives a parent cycle rather than exhausting the stack" and "still rolls a cycle's urgent
  child up to the node that reached it"; both raise `Maximum call stack size exceeded` with
  the guard removed.

- `e454bf0` — **The Projects grid's Tasks column hung on a project cycle.** Found by
  checking whether the pattern above repeated — it did, twice. `taskRatio`
  (`src/lib/tree/taskRatio.ts:30`) walks a subtree with an iterative stack and no `seen` set.
  Reachable because the walk is entered at the project's _own_ id, so it starts inside the
  cycle; a downward walk cannot otherwise enter one, since every member of a cycle has its
  parent within it. Worse than the stack overflow: the loop is synchronous and unbounded, so
  there is no error to surface — the request just never returns. — verified by a test that
  never returned before the fix, and `taskRatio.test.ts` "terminates on a parent cycle" plus
  "counts a task reached twice only once" (the fast signal for the same guard) after.

- `da3fe26` — **Completing a node in a cycle killed the Node process.** Same gap, third site,
  and the one on the _write_ path: `settleDescendants`
  (`src/lib/tree/completionCascade.ts:110`) drains a BFS queue with no `seen` set, in the file
  whose `walkAncestors` ten lines below has carried that guard since it was written.
  `cascadeStateChange` is what Complete runs and `openDescendantCount` is what the
  confirmation dialog counts first — so the dialog that exists to make settling safe was the
  thing that took the server down. Measured, not reasoned about: `FATAL ERROR: Ineffective
mark-compacts near heap limit` in ~8s with `Builtins_ArrayPrototypePush` at the top of the
  trace. — verified by `completionCascade.test.ts` "terminates on a parent cycle instead of
  exhausting the heap".

  **The class is closed.** `derive` (iterative reverse pass, comments the skip), `slice.ts`
  and `notes/slice.ts` (upward via `walkUp`; a cycle costs vanished rows, not a hang) and
  `walkUp` itself were each checked and are already insured.

- `8964789` — **Deleted the Insights panels the envelope rework left behind.** `b134c70`
  (today's envelope release) removed the last use of SpendingTrendsChart, TransactionAudit,
  UpcomingBills and CategoryBars from `InsightsView` but left the files; `git log -S` puts
  every one of the four losses at that commit. RankedBars went with them (CategoryBars was its
  only caller), as did `Panel`'s StatTile and StatRow — same commit — and `ChartLegend` stopped
  being exported now that nothing outside `CashFlowChart` imports it. Worth knowing: the
  component `UpcomingBills` is not the query `loadUpcomingBills`, which the Register page and
  the finance actions both still call — the name collision is why a plain grep makes these
  look live. — verified by typecheck, lint, 4005 unit tests, `next build`, and `npm run smoke`
  (62/62 routes).

- `aaad726` — **Deleted the phone nav's superseded centre capture button.**
  `CaptureNavButton` still described itself as "the bottom nav's centre slot", but `MobileNav`
  has not rendered it since `1ea3642` — Quick capture is a labelled button in the two-action
  utility band beside Process Inbox now. Superseded deliberately, not dropped: I checked
  `MobileNav` before deleting, because a phone that had _lost_ its capture button would be a
  bug rather than dead code. — verified by the same full gate plus smoke.

- `5b98e5b` — **Deleted the Insights view codec its replacement superseded.** The `insights`
  settings scope survives, but `b134c70` swapped its codec to `parseReportSettings`, leaving
  `InsightsViewSettings` + its parse/serialize pair, `DEFAULT_INSIGHTS_VIEW`,
  `CHART_MODE_LABELS`, `insightsFilterOf` and the `INSIGHTS_WINDOWS` alias reading a shape
  nothing writes; `serializeInsightsDrill` and `drillLabel` went with them. Note what stayed
  and why: `INSIGHTS_AXES` / `INSIGHTS_CHART_MODES` look unused as values but three modules
  name the _types_ derived from them — my first cut removed those and typecheck caught it. —
  verified by typecheck, lint, 4005 unit tests, build and smoke (62/62).

- `4b8eb1d` — **Statements sorted account names differently from the Register.** The register
  and Notes each carried an identical private `compareText` (numeric-aware, base-sensitive,
  with an exact-key tiebreaker so two spellings cannot interleave their buckets); Statements,
  written later, used a bare `localeCompare`, so the grid next door put `Account 10` above
  `Account 9` and filed `checking` after every capitalised name. The comparator moved to
  `lib/grid/grouping` as `compareGroupText`, beside the `knownGroupBy` all three already
  share — two copies and one omission became one function. `statementGrouping` also had no
  test file at all; it has six now. — verified by `statementGrouping.test.ts` "orders account
  names the way the register does" (fails with the old comparator, checked), plus new
  `compareGroupText` cases and the full gate including smoke.

- `0a387f8` — **Covered the bill due cue, which had no tests.** `dueCue.ts` decides whether a
  bill about to charge shouts or merely informs and changed twice this week (`527c093` quieted
  it once the envelope is funded) with nothing asserting any of it. Thirteen cases over both
  functions. — checked against four mutants rather than assumed: dropping the past-charge
  guard, `<` → `<=` on the payday comparison, an exclusive horizon, and dropping `overspent`
  from the urgent pair each redden at least one case.

- `33eefe3` — Corrected `derive.ts`'s claim that the L.A.P. memo makes a deep tree "one pass".
  It does not consult the cache for ancestors, so it is one short walk per row. Left the code
  alone (irrelevant at real outline depths); the claim is what needed fixing, since a comment
  asserting the walk is already free is how it would stay unexamined.

- `defd95d` + `017047f` — **A bill with a declared due day printed a raw date key.** Found by
  actually looking at the app, not by any static check: `.artifacts/planner-shots/bills.png`
  shows Rent's Next charge as `2026-09-24` directly above SMECO's `10/2/2026`. `424ba14` made
  that date derived rather than typed, and the derived branch printed `row.node.nextDueKey`
  straight from the row while the typed branch went through `DateKeyCell` and the workspace
  date format. The new Due column was raw for every bill, and `BillFields` — the drawer — had
  the identical split. `bills-fixed.png` shows Rent reading `9/24/2026`, and
  `bills-drawer.png` shows the drawer's `9/24/2026 · due 10/1/2026`, which incidentally
  confirms the `7e16b7e` schedule fix end to end on real data.

  **`017047f` is a correction to my own `defd95d`:** the first fix added a `DateKeyText` to
  `grid/cells`, and `components/date/DateText` already did exactly that job for ten other
  column files — including `budgetColumns`, which renders this same `nextDueKey` correctly on
  the Budget page. Replaced with `DateText`, which gained the one thing it lacked: an optional
  `title` override, because what needs explaining on that cell is where the date came from.

- `da1130e` — **Same class again, and the one no JSX grep could find.** The Accounts grid's
  Freshness column read `As of 2026-09-04 · refresh or import` directly beside Balance as of
  reading `9/4/2026, 6:59:21 PM` — the same instant, two spellings, adjacent columns
  (`.artifacts/planner-shots/accounts.png` → `accounts-fixed.png`). The string is assembled in
  `accountOperations.ts`, pure lib code that cannot reach the date format because it lives in
  a React context — but it already takes `today` as an argument for exactly that reason, so
  the formatter is passed the same way. Made **required**, not defaulted to the identity: a
  default is how the next caller would reintroduce the raw key and see nothing wrong.

- `615a378` — **Same class, another site.** `AmazonReviewDrawer`'s match-candidate list printed
  `row.transactionDate` raw where `amazonColumns` — the grid it opens over — formats every
  date. Found by sweeping `src/components` for the shape rather than waiting to see it; the
  only other hits were URL parameters and `MetricCompactList`, which already formats.

- `98084c8` — **"Still active? · 1 dates to review".** Spotted in
  `.artifacts/planner-shots/bills.png`. Swept every `{count} nouns` in the components: most
  were already guarded by a `=== 1` branch, six were not and each can legitimately be one —
  the Bills review count, Insights' uncategorized "N rows", the account drawer's transaction
  count, the account and payee delete confirmations, and the funding plan's "N details" —
  plus Supplies' mixed title, whose single-kind branches are right but whose "1 items and 3
  offers" falls between them. Read back from the live page as "1 date to review".

- `d3829e3` — **Fifth and last raw-date-key site.** The balance strip above the Register
  carried a hover reading `Statement $764.46 as of 2026-07-31.` `AccountBalances` is a
  component, so unlike the Accounts label it can call `useDateFormatter` directly. Read back
  from the live page: "Statement $764.46 as of 7/31/2026."

- `9078c32` — **`docs/agent-api.md` was stale.** `424ba14` gave `upsert_subscription` and
  `save_subscription` a `leadDays` argument this afternoon without re-running
  `npm run agent:docs`, so an agent reading the doc could not know the field exists.
  `agent:docs:check` has been failing since; I verified it also fails at `80e5b2d`, so it is
  not fallout from tonight.

- `067aa83` — **`format:check` was red, also from before tonight.** Four files; this is the
  one that is code. See _Needs a decision_ for the other three.

- `9e459a9` — **Closed the first follow-up at the end of
  `specs/2026-09-05-1506-grid-column-track-model`.** The tree drop line and child-drop marker
  read _declared_ column widths and ignored stored overrides, so resizing any column before
  the name column left them pointing at the wrong depth. The cause is two places working out
  how wide a column really is: `buildGridTemplate` honoured an override, while
  `nameColumnLeft` sat in `DataGrid` with its own copy and could not even see `widths` — it
  ran per row, and rows are not given them. One shared `resolvedTrack` now answers it, with
  `nameColumnOffset` beside `buildGridTemplate` in `lib/grid/template`, where that spec had
  already decided this arithmetic belongs. Falls out of the move: the offset is computed once
  per grid rather than rebuilt per row, `DataRow` stops needing `handleWidth`, and the 0.75rem
  cell gap becomes `CELL_GAP` — the offset must add up the same gaps the row lays out. —
  verified by five new `template.test.ts` cases (including the resized case), and in the
  browser by confirming rows still re-render: click plus ArrowDown moves the selection.

  I did **not** edit the frozen spec to tick the follow-up off — a frozen folder is a record.
  The other two items there (hand-picked default widths, the 4px resize target) are taste
  calls and stay open.

- `306333f` — **Closed the second follow-up in
  `specs/2026-09-02-1050-retire-tags-and-legacy-category`:** `GridFilterValue` collapses from
  `string | readonly string[] | null` to `string | null`. Still stale, and more narrowly than
  the spec estimated — every producer already declares `(row) => string | null`, and the only
  code branching on the array was `scalarFilterValues` itself, which needed an `as string`
  cast to convince the compiler of what the types already guaranteed. `filters.ts`,
  `search.ts`, `crossFilter.ts` and `customFilter.ts` only call the helpers, so they needed no
  edit; the `Array.isArray` calls remaining in `crossFilter` and `exportCsv` are about
  persisted JSON and CSV cells, not this type. `filterValue.ts` had no test file and has one
  now, pinning what the collapse must not lose: null and `""` are both _absent_, whitespace is
  not. — verified in the app too: searching Tasks for "bench" narrows 45 rows to "Do bench
  press", and clearing returns to 10.

- `dbc2a04` — **Pinned where the short equipment tag differs from the badge.**
  `formatEquipmentShort` was the one function in `fitness/equipment.ts` with no test, and it
  diverges from `formatEquipmentBadge` in exactly two places: plain "Barbell" for a standard
  bar where the badge says "Barbell · Olympic 45", and L/R joined by a space where the badge
  uses a middot. Nothing asserted either, so folding the two together — the obvious tidy-up —
  would have passed the suite while changing every picker tag in Fitness. The tests assert
  both spellings side by side so the difference reads as intended rather than as drift.

## Verification sweeps (found nothing, which is the point)

Two automated passes over the running app, both clean — worth recording so nobody re-does
them:

- **Every route's rendered text**, desktop (23 routes) and phone at 390×844 (10), scanned for
  raw `YYYY-MM-DD`, `NaN`, `undefined`, `[object Object]`, and singular-as-plural. After the
  five date fixes the only remaining ISO hits are two false positives: Settings' date-format
  picker showing its own `YYYY-MM-DD` sample, and imported RedNotebook journal _content_ on
  `/notes/grid`. So that class is now genuinely closed, not just closed where I happened to
  look.
- **Console output on 24 routes**: zero errors and zero warnings — no hydration mismatches,
  no React warnings. `npm run smoke` only proves the server rendered; this is the client half.

## Tried and backed out

- **I dirtied your Tasks view and put it back.** Driving the real database, I typed "python"
  into the Tasks search to test the filter path — and grid search is _persisted_
  (`user_settings`), so it survived the page load and left Tasks reading "Showing 0 of 45".
  For a few minutes I thought I had broken filtering. I cleared it through React's native
  input setter and confirmed on a fresh load that the box is empty and the count is back to
  "Showing 10 of 45", which is what it read before I touched it. Nothing else about the view
  changed — the `State: all but Completed` chip and the "Unsaved changes" mark were both
  already there. Worth knowing for next time: on this app a browser test that types into a
  toolbar writes to your settings.

- Two attempts to open the Outline and Contacts drawers from the driver failed
  (`[role="dialog"]` is not what they render, and a double-click on the Outline name cell
  starts a rename rather than opening). I stopped after the second rather than dig — the Bills
  drawer opens fine and is what the date work needed.

- Nothing was reverted. Two things were narrowed mid-cycle rather than abandoned: the first
  cut of the Insights settings removed `INSIGHTS_AXES`/`INSIGHTS_CHART_MODES` too, and
  typecheck caught three modules still naming the types derived from them; and the
  five-copy grouping-engine refactor was scoped down to the comparator once I confirmed the
  five engines do not actually disagree (see below).

## Needs a decision

- **Three frozen specs fail `prettier --check`** —
  `2026-08-14-1012-recurring-bill-cadences`, `2026-08-18-2058-commitments-clarity` and
  `2026-08-25-0901-bill-next-charge`, all `plan.md`. The drift is whitespace and changes no
  intent, so normalizing is safe in substance — but "no reopening a frozen spec" is a standing
  rule and a frozen folder is a historical record, so I reverted the change rather than decide
  it alone. Nothing is blocked: `format:check` is in neither a git hook nor CI.
  **My recommendation:** run `npx prettier --write agent-os/specs/**/plan.md` and let
  `format:check` go green, since a permanently-red gate is one nobody reads. The alternative —
  a prettier ignore for frozen folders — also works and is more faithful to the rule.
  — blocking: nothing.

- **CI does not run `npm run build`, and nothing else does either.** `.husky/pre-commit` runs
  lint / typecheck / unit; `.husky/pre-push` runs the full suite; `.github/workflows/ci.yml`
  runs lint, typecheck and `npm test`. None builds. `AGENTS.md` says the build "catches things
  the other three don't", and the workflow's own header says it exists for "branches that
  never touch Lee's machine" — which is exactly the Dependabot case where a bump that breaks
  the build would go green in CI and fail at the Vercel deploy.
  **My recommendation:** add `- run: npm run build` after `npm test`. Two things I checked
  that make it look safe: `scripts/migrate-on-deploy.mjs` exits 0 immediately unless
  `VERCEL_ENV === "production"`, so it is a no-op in CI; and the only module-scope env a build
  needs is `DATABASE_URL` (`src/db/index.ts:19` calls `connectionString()` at import), which
  the job already sets.
  **Why I did not just do it:** I cannot run a GitHub Actions job from here, and proving the
  build works without a `.env.local` would have meant moving Lee's `.env.local` aside on a
  machine I am unattended on. Verifying that is a two-minute job for whoever is awake; making
  CI red overnight is not worth the guess.
  — blocking: nothing today, but it is the difference between Dependabot being gated and
  looking gated.

- **53 exports in the tree have no reference anywhere, including their own file.** The list is
  reproducible with a short script; the biggest coherent clusters are twelve `"use server"`
  actions in `src/app/finances/actions.ts` and `src/app/fitness/actions.ts`, three
  `moduleHasPageBar`/`moduleDefaultPageHref`/`moduleDeclaredPages` in `shell/modules.ts`, and
  the `parseSchedules`/`serializeSchedules`/`SCHEDULES_SCOPE` trio orphaned by `58f05f3`
  ("Collapse Budget, Schedules and Commitments into one budget").
  **My recommendation:** delete the Schedules trio and the module helpers, but treat the
  server actions as a separate question — an unused action is either dead or an API surface
  you meant to keep, and I cannot tell which from the code. I did not touch any of them
  tonight because, unlike the Insights leftovers, I could not trace each to a commit that
  clearly superseded it. `SCHEDULES_SCOPE` additionally appears in `SCOPE_KINDS`, which the
  settings reset surface enumerates, so removing it changes what "Reset everything" walks —
  worth a deliberate look rather than a 3am guess.
  — blocking: nothing; it is hygiene, not a blocker.

- **A separate ~130 exports are used only inside their own file** (`DATE_PRESETS`,
  `authClient`, `FINANCE_CATEGORIES`, `isSpending`, …). Not dead — over-exported. Narrowing
  them is a real readability win and a mechanical change, but it is ~130 edits across the
  whole `lib` tree and would collide with anything you have in flight, so it wants to be its
  own deliberate pass rather than a slice of someone's night.
  — blocking: nothing.

## Noticed but didn't touch

- **Two of the three follow-ups in `specs/2026-09-03-0951-cancelled-bill-handling` prescribe
  opposite treatments for the same state** — "Mark `(cancelled)` in the Register picker"
  (keep it, label it) and "Omit cancelled from Move money / Assign Manual destination
  catalogs" (hide it). Both are one-line bullets. Which surfaces mark and which omit is a
  product call, and getting it backwards is worse than leaving it, so I did neither. The
  third, "Auto-assign leftover out of a cancelled bill", moves money and is plainly yours.

- **`Supplies` shows "Pets · funded from Geico · budgeted $0.00/mo"** — cat food charged to
  the car-insurance envelope. I chased this as a possible name-collision bug, because
  `SuppliesView:170` resolves a group by `group:${label}`rather than by id, which is exactly
the fragility`b134c70`removed from the bills. It is **not** a bug:`supplyGroups`keys a`Map` by label, so labels are unique by construction and the lookup cannot collide, and the
  envelope shown is the one every item in the group actually points at. So it is your data,
  not the code — but the line reads wrong enough to be worth a look.
- **The Task Chooser shows rank `#1` at the bottom of the list**, under `#20`
  (`.artifacts/planner-shots/chooser.png`). Also not a bug: the grid carries a stored
  `PRI ↑` sort (the toolbar says "Unsaved changes"), the Chooser declares no default sort, and
  `<New Tasks>` has no priority so it sorts last. Clearing it from the column header menu puts
  the score order back. Worth knowing only because a numbered rank column displayed out of
  numeric order looks like a bug at a glance.
- **Two batch summaries still say "1 result areas" / "1 bills"** —
  `AchieveTransferPanel.tsx:103` and `AmazonSnapshotPanel.tsx:45`. My plural sweep's regex
  missed the `${counts.x}` shape, so `98084c8`'s message slightly overstates its coverage.
  Left deliberately: both are one-line summaries listing four counts at once, the line above
  the first already uses the `(s)` convention, and picking between four inline ternaries and
  four `(s)` suffixes is a taste call rather than a fix.

- **Five copies of the same grouping engine** — `src/lib/tree/slice.ts:724`,
  `amazon/grouping.ts:159`, `notes/grouping.ts:271`, `finances/grouping.ts:218`,
  `finances/statementGrouping.ts:108`. Identical stack/`closeTo`/header-emit loop; only the
  comparator and part extraction legitimately differ. I checked them against each other and
  they do **not** disagree, so there is no bug hiding in the duplication — which is why I left
  it rather than spending the night on a five-file refactor nobody reviewed. Worth an extracted
  `groupRows(rows, dims, {partOf, compare, emptyLabel})` in daylight.
- `src/lib/finances/statementGrouping.ts:56` — sorts account names with a bare
  `localeCompare`, where the register (`grouping.ts:100`, `compareText`) sorts them
  numeric-aware and case-insensitively. Same account names, two orders across two grids.
  Would fall out of the extraction above.
- `src/lib/finances/statementGrouping.ts` has no test file at all.
- `src/lib/tree/derive.ts:31` — the comment claims the L.A.P. memo makes a deep tree "one
  pass rather than one walk per node", but `lapFor` never consults the cache for ancestors, so
  it is O(N·depth), not O(N). Harmless at real outline depths; the comment is just wrong.

## Gate status at end of run

Run at 22:56, on `da1130e`, whole repo:

| gate               | result                                        |
| ------------------ | --------------------------------------------- |
| `typecheck`        | pass                                          |
| `lint`             | pass (`--max-warnings=0`)                     |
| `test:unit`        | pass — 340 files, 4028 tests                  |
| `test:integration` | pass — 62 files, **1019 tests, actually ran** |
| `build`            | pass                                          |
| `smoke`            | pass — 62/62 routes rendered                  |

**The database tests really ran** — no `⚠ Skipping` line; `planner-postgres` was already up
and I checked for the skip warning after every run that touched a `queries.ts`/`mutations.ts`
path. The tree is clean and everything above is pushed to `origin/master`.

Working tree clean, nothing stashed, nothing left half-done.

One process note: `run-planner`'s gotcha says `npm run build` and `npm run dev` fight over
`.next`, and a dev server was already running on 3047 when I started (I did not start or stop
it — it may be yours). I ran the build several times anyway before reading that. It did not
bite — `npm run smoke` passed after every one, and the browser driver kept rendering — but if
the dev server behaves oddly in the morning, restarting it is the first thing to try.
