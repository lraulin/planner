# Overnight run — 2026-09-12

Started 20:25. Focus: self-directed.

Prior runs swept finance, cross-user scoping, dead exports and the spec D-lists until they came
back dry, so tonight starts on the task side: recurrence and the date code around it.

## Shipped

- `16ed43dd` — **The task drawer's "Next" recurrence preview could name a date completing would
  never produce.** `RecurrenceFields.tsx` said it used "the same precedence
  `applyStateTransition` uses" but kept its own copy — deadline, then _any_ deferred date, then
  target start. Completing skips a deferred date that has already expired (that residue once
  jumped target start from 2020 to 2033), so a repeating task with an expired deferred date and
  a cleared or separately-set target start previewed from the stale date. The same wrong date
  also seeded the pattern defaults (weekday, day of month) when you switched pattern. The rule
  now lives once in `src/lib/recurrence/anchor.ts`, called by completion, Skip and the preview.
  Honest scope: after a normal cycle deferred and target start move together, so the two
  precedences agree in the common case — this bit only on hand-edited dates. Verified by
  `anchor.test.ts` (5 cases; flipping `>` to `>=` fails exactly the "deferred today is
  expired" case) and the 113 tree integration tests. **Not browser-verified** — the only
  server is yours on 3047 against real data, and the drawer autosaves.

- `31a213ef` — **A monthly appointment on the 31st sat on the 28th forever after its first
  February** (Jan 31, Feb 28, Mar 28, Apr 28…), and a yearly Feb 29 series never returned to
  the 29th. `expandRecurrence` stepped each monthly/yearly occurrence from the one before, and
  the month clamp is lossy. It now measures occurrence _i_ from the series start. Same class as
  09-10's bill fix (`1e934bf`), in an unrelated module; task recurrence (`pattern.ts`) and bills
  were already immune, and this was the last path that compounded the clamp. The old test
  pinned the drifting dates "so a change is deliberate" — it came in with the bulk
  gate commit `b772e04c`, no Achieve rule backs it, and both of the app's other recurrences
  disagree, so I replaced it. Verified by five tests that fail on the old code: the clamp
  itself, the 31st in mid-2027, a six-month interval from Aug 31, a Feb 29 leap-year return,
  and an end-after-3 count.

- `6fe71629` — **A repeating appointment that was still running when a calendar range opened
  was left off it.** A daily 22:00–06:00 lost its after-midnight half on every range, and a
  weekly Saturday-night one vanished from Sunday morning of the week view. Both fast-forwards
  in `expandRecurrence` skipped every occurrence that had _started_ before the range, when the
  question is whether it had _ended_; the overlap check further down was already right and
  never saw them. Found the weekly half only because my own test comment claimed "the same
  way a weekly series already does" and I probed it before committing — it didn't. Scope
  honestly: `/schedule/day` and Agenda bucket by start day (`day/appointments.ts:34`), so this
  shows on the calendar grid, not those. Verified by four tests that fail on the old code,
  including an end-after-2 weekly series that reaching back must not revive.

- `0af4841a` — **Every date in a YAML export came back as a timestamp, not text.** `yamlScalar`
  promises to quote any cell a bare scalar would turn into a different value, but left `~`,
  `2026-09-12`, `.inf`, `.NaN`, `0x1F`, `0o17` and `+12` bare. I loaded each through js-yaml
  rather than trusting my memory of the spec: null, a `Date`, and five numbers. It matters
  because the shared Deadline column and the Timeline export raw `YYYY-MM-DD` keys
  (`commonColumns.tsx:132`, `timelineColumns.tsx:66`). One test, fails on the old code.

- `2033044f`, `684cf32f` — **Two leftovers from finished migrations.** `csvFilename` was kept
  "so existing CSV call sites stay obvious" and its only caller was its own test.
  `OutlineColumnCtx.selectedId` was kept optional "during the selection-context migration";
  no grid sets it and no column reads it. Those were the tree's only two `@deprecated` markers.

- `2c0f0272` — **A note made after ~8pm Eastern was dated tomorrow.** Notes ▸ New note and a
  record's linked-notes button both call `createNote` without a date, and it defaulted to
  `new Date()` — an instant, in a column `dates.md` says holds a calendar day as UTC noon. So
  21:30 Eastern (01:30Z) read back through `toDateKey` as the next day in the Date column and
  the Journal, and matched tomorrow's Day page via `noteDate::date`. This was wrong on the
  laptop too, not only on Vercel. Journal, organizer and both importers already passed
  `fromDateKey`; this default was the only writer that didn't. Fixed twice over: the default
  stores the calendar-day encoding, and the two buttons send the reader's local day, since the
  server's "today" is UTC in production. Found by grepping for the anti-patterns `dates.md`
  names rather than reading modules. Verified by an integration test that fakes only `Date` at
  01:30Z (fails on the old code with `2026-09-12`), the notes suite, and `npm run smoke` (62
  routes).

- `92236e78` — **The plate hint said "bar only (45 lb)" beside a 30 lb set.** `formatPlateLoad`
  treated every total at or below the bar as bar-only, guarded by `remainder === 0` — but
  `calculatePlates` computes that remainder as `max(0, total − bar)`, always 0 in that branch,
  so the guard never guarded. The only test was exactly 45. Now only the bar's own weight
  reads bar-only; a lighter load gets no hint, the same as a load no plates can make. One
  test (lb and kg), fails on the old code.

- `061b8d1f` — `AGENTS.md` said `npm run smoke` "loads all 23 routes"; it loaded 62 tonight.
  Smoke discovers routes itself, so the number is gone rather than updated.

## Tried and backed out

## Needs a decision

- **Per-user timezone is still the biggest open date question** (08-07, 08-25, 08-30). Nothing
  new tonight except a count: 40-odd server-side `localDateKey(...)` calls read the server's
  zone, including `applyStateTransition`'s completion day (`tree/mutations.ts:930`) — whose
  comment says it exists to beat "evening in the Americas" and only does so where the process
  runs in the user's zone. The test suite pins `TZ=America/New_York`, so nothing in the gate
  can see it. Recommendation unchanged: store an IANA zone on the account.

- **A Proposed item reads "Ongoing" in the Status column.** `scheduleStatus` computes
  `started = effective !== "not_started"` (`tree/status.ts:108`), so every state other than
  Not Started counts as started work — including `proposed`, which `date-model.md` defines as
  "uncommitted incubation — idea / Someday". A Someday project with no dates therefore shows
  Ongoing, Achieve's word for "started, no near end date"; give it a target end last week and
  it reads Behind Schedule for being unfinished. Achieve's manual only says "has been started",
  and its Current filter treats _In Progress and Should Delegate_ as the ongoing states.
  Options: (a) count `proposed` as not started — it then reads Not Scheduled with no dates,
  which fits Someday, but Need to Start / Behind Schedule once it has a past target start;
  (b) give proposed its own early return, like `waiting`, so no plan band applies until you
  commit to it; (c) leave it. **Recommendation: (b)** — an uncommitted idea is not behind on
  anything — but it changes what a status filter matches, so it's yours. `delegated` has the
  milder version of the same question. — blocking: nothing, one-line change once decided.

- **Calendar events in five Google colours get white text that fails even the large-text
  contrast floor.** `contrastText` (`schedule/geometry.ts:242`) switches to light text below
  relative luminance **0.45**; the point where white and black give equal contrast is
  **≈0.179** (`√(1.05 × 0.05) − 0.05`), so everything between reads with the worse ink. I
  computed WCAG ratios for Google's event palette with the app's actual inks (`#f5f5f7` /
  `#1b1d23`):

  | Colour                | White text now | Dark text instead |
  | --------------------- | -------------- | ----------------- |
  | Sage `#33b679`        | 2.38           | 6.50              |
  | Flamingo `#e67c73`    | 2.57           | 6.01              |
  | **Peacock** `#039be5` | 2.83           | 5.47              |
  | Tangerine `#f4511e`   | 3.19           | 4.85              |
  | Lavender `#7986cb`    | 3.17           | 4.88              |

  The other six Google colours and all ten Time Chart presets already get the better ink, and
  stay unchanged at 0.179 (the four existing tests pass either way). **Why I didn't ship it:**
  Peacock is Google's default calendar colour, so this flips the text on most events you look
  at, and Google Calendar itself draws white on these fills — `WeekCalendar.tsx` says it is
  matching Google's look. The docstring says the goal is "readable", and the numbers say
  0.179 is readable; whether that beats looking like Google is yours. **Recommendation:**
  0.179, one constant plus a test naming Peacock. — blocking: nothing.

## Noticed but didn't touch

- **A day-31 monthly appointment means two different things depending on whether Google is
  linked.** Local series now clamp (Feb 28, Mar 31, Apr 30). A series created with a push
  target goes to Google as `FREQ=MONTHLY` (`google/mapping.ts:215`) and comes back as Google's
  own expansion, which follows RFC 5545 and **skips** months with no 31st — so no February,
  April, June, September or November occurrence at all. The monthly preview can't tell you
  which you'll get. Options: send `BYMONTHDAY=-1` when the start is the last day of its month
  (matches the clamp for the 31st only, not the 29th/30th), or accept the split. It only bites
  on days 29–31, so I'd leave it unless you create such a series.

- **Two more places that only work because you live west of Greenwich.** Neither is worth a
  patch on its own; both belong to the per-user-timezone decision above.
  - `recurrence/pattern.ts` builds its next occurrence as a **local midnight**
    (`new Date(y, m − 1, d)`, lines 94/194/224 — the form `dates.md` rule 4 forbids in shared
    code) and callers re-encode with `toDateKey`. In UTC and every zone west of it, local midnight
    is still that day in UTC, so it reads right; anywhere east of UTC it reads as the day before.
  - `expandRecurrence`'s until check is `toDateKey(start) > toDateKey(recurrenceUntil)`
    (`schedule/recurrence.ts:113`). `start` is a timed **instant**, so on the laptop a 7pm
    Eastern occurrence on the until day (00:00Z the next day) is dropped as past the end. On
    Vercel the expansion runs in UTC anyway, so switching to `localDateKey` would not help
    production. Only local rules hit it; Google series arrive as instances without one.

## Gate status at end of run
