# Overnight run — 2026-08-11

Started 20:49 EDT. Focus: DRY / clean-code — keyboard command labels already moved
into `chords.ts` (`c3060f2`); hunt remaining duplication and other unambiguous
hygiene.

## Shipped

- `5b981c6` — **Name the rest of the keyboard scheme and stop retyping glyphs.**
  `chords.ts` now owns movement, clipboard, expand, Achieve verbs, Save, form-commit,
  palette open, and quick-capture. `commandDeck`, HintBar, Drawer tooltips/listeners,
  Organizer Process, Sidebar Search, CommandPalette, and QuickCapture all read bindings
  from there via `formatBindings` / `matchBindings`. The outline HintBar no longer keeps
  a hand-typed list that can lag a scheme change (that is how Insert stayed on the banner
  after Return took over). Form commit and insert-after share one `META_ENTER` object so
  they cannot drift. — verified by `chords.test.ts` + `commandDeck.test.ts` + full unit
  (1791) + typecheck + lint + build + integration (2302)

- `757caef` — **Share `isSettled` and test the Projects task-ratio walk.** The Projects
  grid computed open/total with a private `isActive` and an untested subtree walk.
  Walk lives in `lib/tree/taskRatio` (nested projects, settled states). Hand-rolled
  `completed || cancelled` checks in derive, name cell, schedule projects rail, destination
  picker, and agent outline/plan/search now call `isSettled`. — verified by
  `taskRatio.test.ts` + 1795 unit + integration (2306)

- `bf4d97f` — **Markdown bold/italic join the scheme; more `isSettled` call sites.**
  Editor chrome and listeners use `MARKDOWN_BOLD` / `MARKDOWN_ITALIC`. Day sync, day
  mutations, capture inbox reopen, schedule status, shelf effective state, and Day view
  optimistic settle call `isSettled`. Day-line settlement keeps `isDayItemSettled` (stamp
  can exist without node state). — verified by status/chords/day/editing tests + unit gate

- `8ef3292` — **Day title and week planner use `isDayItemSettled` for strikethrough.**
  Both had restated `completedAt || state === cancelled`, which missed a completed line
  whose stamp was missing. Shared helper + test for that case. — verified by
  `priority.test.ts` ("treats completed state…") + full unit/integration (2307)

- `ea24dd9` — **Day swipe and Metrics tracking help use the shared helpers.** Swipe-right
  Complete/Reopen asked `completedAt || state === completed` beside a separate cancelled
  branch; it now uses `isDayItemSettled`. Metrics tracking-table help printed `⌘C` by hand;
  it asks `COPY_AS_TEXT`. — verified by unit gate

## Tried and backed out

_(none)_

## Needs a decision

- Completing a project from the **detail drawer** vs from a **grid** still diverges:
  grids confirm and cascade-settle open descendants; the drawer calls
  `applyStateTransition` + upward-only `reopenSettledAncestors` and leaves children open.
  Prior overnight recommended adding `openDescendantCount` to the detail payload (option a).
  Still needs acceptance criteria as a small delta-spec.
  — blocking: consistent Complete on every surface

- **Empty-state chrome:** Outline / Metrics / Fitness / Notes each own a local
  `EmptyState` with different filtered-vs-empty branches and actions. Clean-code says
  tolerate duplicate _shape_; extracting a shared shell needs a product call on whether
  the empty copy should feel identical across modules.
  — blocking: nothing urgent; cosmetic consistency only

- **Note delete wording vs node delete wording:** notes say "removed" and name notes under
  the row; outline nodes say "deleted" and "items under it". Unifying into one cascade
  helper needs a deliberate shared sentence, not a silent merge.
  — blocking: nothing; both are correct domain prose today

## Noticed but didn't touch

- Selection gestures in HintBar (`⇧click`, `⌘click`, arrows) have no binding object — they
  are pointer/selection, not commands. Left as prose.
- Metrics tracking help still hardcodes "⌘-click" for multi-select (no binding object);
  the copy half now uses `COPY_AS_TEXT`.
- `day/forward.ts` still lists `SETTLED: NodeState[] = ["cancelled", "completed"]` for an
  `.includes` filter on a thin candidate type — same pair as `isSettled`, left alone so
  the candidate type stays free of the cascade module.
- `contacts/queries.ts` `SETTLED_STATES` for SQL `notInArray` — array form required by
  Drizzle, not a predicate.
- Agent `getContext` / `searchNodes` still load the full outline (by design; filtering is
  in-memory). `get_node` already uses ancestor chain only.
- Local `toLocaleDateString` / `toLocaleString` in DayHeader, MiniMonth, Fitness
  `formatWhen`, planning week labels — intentional wall-clock formatting, not the user
  date-format preference used in grid columns.
- Roadmap residual: find-in-outline, Pomodoro, GTD Someday-Maybe remain morning work.

## Gate status at end of run

| Check       | Result                                                           |
| ----------- | ---------------------------------------------------------------- |
| typecheck   | pass                                                             |
| lint        | pass                                                             |
| unit        | pass (1796)                                                      |
| integration | pass (2307 total including unit) — Postgres was up; DB tests ran |
| build       | pass (before first commit; no routing/config change after)       |

Tree clean on `a66b722`. Five work commits + two report commits pushed to `origin/master`.
