# Overnight run — 2026-08-11

Started 20:49 EDT. Focus: DRY / clean-code — keyboard command labels already moved
into `chords.ts` (`c3060f2`); hunt remaining duplication and other unambiguous
hygiene.

## Shipped

- _(pending commit)_ — **Name the rest of the keyboard scheme and stop retyping
  glyphs.** `chords.ts` now owns movement, clipboard, expand, Achieve verbs, Save,
  form-commit, palette open, and quick-capture. `commandDeck`, HintBar, Drawer
  tooltips/listeners, Organizer Process, Sidebar Search, CommandPalette, and
  QuickCapture all read bindings from there via `formatBindings` / `matchBindings`.
  The outline HintBar no longer keeps a hand-typed list that can lag a scheme
  change (that is how Insert stayed on the banner after Return took over). Form
  commit and insert-after share one `META_ENTER` object so they cannot drift.
  — verified by `chords.test.ts` + `commandDeck.test.ts` + full unit (1791) +
  typecheck + lint + build

## Tried and backed out

_(none yet)_

## Needs a decision

_(none yet)_

## Noticed but didn't touch

- Local `EmptyState` helpers in Outline / Metrics / Fitness / Notes — similar
  shell JSX, different filtered branches and actions. Clean-code says tolerate
  duplicate _shape_; not extracted.
- Notes `deleteMessage` parallels `nodeDeleteMessage` with note-specific wording
  ("removed" / "notes under it"). Unifying needs a deliberate sentence for both.
- MarkdownEditor still hardcodes `⌘B bold · ⌘I italic · Tab indent` as chrome
  text; those are editor-local and not in the command registry.

## Gate status at end of run

_(in progress)_
