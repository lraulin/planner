# Overnight run — 2026-08-25

Started 01:33. Focus: self-directed.

## Shipped

- `7e29ea9` — **Read finance today from the wall clock, not UTC.** Budget, Dashboard, Register, `loadBudget`, Review candidates, and the one-budget cutover used `toDateKey(new Date())` for "today". That helper is for stored calendar fields (UTC noon). After about 8pm Eastern, UTC is already tomorrow, so the current month, upcoming bills, and Register year-collapse all jumped a day. Swapped to `localDateKey`, matching the rest of the app and `dates.md`. On Vercel the process is still UTC, so production evenings remain wrong until a per-user timezone exists (open since 2026-08-07). — verified by `geometry.test.ts` 9pm EDT case (`localDateKey` Aug 25 vs `toDateKey` Aug 26), typecheck, lint, 3193 unit, 860 integration (Postgres up), `next build`.

- `015b90c` — **Stop counting the next month's 1st as this month's bill.** Weekly/daily bill demand walked occurrences with an exclusive end of `monthKeyOf(...) + "-01"`. `MonthKey` is already `YYYY-MM-01`, so August's end became `"2026-09-01-01"` and `"2026-09-01"` compared as still inside August. A weekly bill due on the 1st was demanded five times in the prior month ($100.00 instead of $80.00 at $20/week). Uses `nextMonthKey`. — verified by `schedule.test.ts` "does not count the 1st of next month" (failed 10000 vs 8000 before the fix), typecheck, lint, 3194 unit.

- `8ada068` — **Pin group collapse and empty-header recounting.** `applyGroupCollapse` / `dropEmptyGroups` had no sibling tests. A wrong depth comparison would hide a sibling after a collapsed parent, or leave a header reading "Career (7)" above one visible row. — verified by `collapse.test.ts` (6 cases), typecheck, lint, 3200 unit.

- `398d4cf` — **Say `dayString` decodes a stored calendar day, not today.** The helper already called `toDateKey`; its JSDoc claimed wall-clock day of a timestamp, which is the same pitfall as the finance pages. Callers pass UTC-noon plan dates, so scoring was not jumping a day — the comment was the trap. — verified by `chooser/dates.test.ts` evening case, typecheck, lint, 3201 unit.

- `4eff191` — **Pin sinking-fund month distance to the calendar month.** `monthsUntilDate` keys a due date to `YYYY-MM-01`. Using the day of month would make a 15th look farther away than the 1st of the same month, and yearly bill demand (remaining / months-until-due) would drift. — verified by `monthSpan.test.ts`, typecheck, lint, 3203 unit.

- `1252abe` — **Pin that remainder is not an assign ask.** `demandOf` is what Underfunded reads. Remainder is leftover Ready to Assign, not a request; a bill with no next-due must error instead of asking zero, which would look funded. — verified by `demand.test.ts`, typecheck, lint, 3207 unit.

- `f68a5cb` — **Drop the unused todayKey from the one-budget cutover.** It was computed then `void`ed; spend envelopes take their simple template from the pinned rate or weekly median. — verified by typecheck, lint, 3207 unit.

## Tried and backed out

- MCP OAuth well-known routes looked missing from `list_dir` (dot-folders hidden). They already exist at `src/app/.well-known/oauth-authorization-server` and `oauth-protected-resource`, proxy allows them, `smoke-agent.mjs` hits them. Spec checkboxes still open because Grok.com connector verification is Lee's.

- `dropEmptyGroups` appeared to keep an empty header over a later ungrouped node. That fixture was ill-formed: a node after a group with no following sibling header _is_ a member of that group. Corrected the test; no production bug.

## Needs a decision

- **Per-user timezone (still open from 2026-08-07).** `localDateKey(new Date())` on the server is correct on the laptop (`TZ=America/New_York`) and wrong on Vercel (UTC) every evening ~20:00–midnight Eastern. Tonight's finance swap stops using the _even worse_ `toDateKey(new Date())`, but `/day`, detail mutations, `startOfWeek` / `weekly_plans.week_start`, and Google `timeZone: "UTC"` are unchanged. Recommendation remains: store IANA zone on the account, defaulted from `Intl.DateTimeFormat().resolvedOptions().timeZone` at first sign-in.
  — blocking: correct evening `/day` on the phone; DST expansion of recurring appointments written to Google; finance "today" on the deployed app.

- Completing a project from the **detail drawer** vs from a **grid** still diverges (grids confirm and cascade-settle; drawer is upward-only). Prior overnights recommended `openDescendantCount` on the detail payload. Still needs a small delta-spec.
  — blocking: consistent Complete on every surface.

- **MCP OAuth spec is implemented but still `Status: active`.** Acceptance criteria in `2026-08-13-1805-mcp-oauth` are unchecked; routes, metadata, PKCE token exchange, and `smoke-agent` "OAuth discovery is public" exist. Freeze is Lee's once Grok.com actually connects.
  — blocking: closing that spec.

- **Earmarked savings / Goals join / Pomodoro / attachments / GTD Someday** — roadmap still asking questions. Left alone.

## Noticed but didn't touch

- `src/lib/finances/budget/cutover.ts:419` — `void todayKey;` leftover after spend migration; todayKey is computed and unused.
- `src/lib/finances/commitments.ts:369` — a _local_ `monthKeyOf` returns `YYYY-MM` (not envelope `YYYY-MM-01`). Internally consistent with `monthStart`/`monthEnd` there; do not merge with envelope keys without a conversion.
- `src/lib/finances/budget/templates/demand.ts` — Bill + leftover `simple` templates each see the same carry-in; bills usually have empty `templates`, so stacking is latent. Sibling tests now cover remainder-not-an-ask and missing next-due.
- `src/lib/chooser/dates.ts` `dayString` JSDoc was the trap; callers already passed UTC-noon fields so scoring was not jumping a day.
- `src/app/finances/dashboard/page.tsx` already documents that the day-count is not computed on the server, then still used `toDateKey(new Date())` for month and upcoming bills (fixed tonight).
- Drawer vs grid Complete cascade — still the 2026-08-11 decision.
- `src/lib/notes/derive.ts` — forward pass, not recursive; overnight 2026-08-12 left it.

## Gate status at end of run

typecheck — pass  
lint — pass  
unit — pass (3207)  
integration — pass, 860 tests, Postgres was up (`planner-postgres Running`); no skip warning  
build — pass (`next build` after the finance page change)

DB tests actually ran.
