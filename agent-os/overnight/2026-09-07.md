# Overnight run — 2026-09-07

Started 21:13. Focus: self-directed.

## Shipped

- `5cb5b4c` — Deleted four dead finance server actions (`listTransactionsAction`,
  `deleteTransactionAction`, `autoMapBudgetAction`, `setTaxonomyCategoryEnvelopeAction`)
  and the retired `setTaxonomyCategoryEnvelope` no-op behind the last one. The first was
  the explicit follow-up of `specs/2026-08-24-1945-register-prepared-rows`; the other
  three had gone dead the same way without being written down. A `"use server"` export is
  a public endpoint, so each was reachable surface with no reader. Verified by full gate
  including 1021 integration tests and `npm run build`.

## Tried and backed out

## Needs a decision

## Noticed but didn't touch

## Gate status at end of run

_(pending)_
