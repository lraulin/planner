# Overnight run — 2026-09-10

Started 21:22. Focus: self-directed.

## Shipped

- `a30c2a8` — **The Metrics performance graph drew every label squashed.** Since the pane became
  resizable (`86f71e5`, 2026-08-02) the chart has been a fixed 640×240 viewBox stretched into
  the pane with `preserveAspectRatio="none"`, which scales x and y independently. At the default
  desktop pane (measured 1502×156) x grows 2.35× and y shrinks to 0.65×, so each axis label was
  3.6× wider than tall and each marker an ellipse — the component's own comment had noticed the
  ellipse and widened the hit area instead. The plot helpers already take width and height, so
  the graph now measures its container and draws at 1 unit = 1 px; the date-axis tick count
  follows the real width so a narrow pane asks for fewer labels. The Timeline ribbon's
  `useElementWidth` became the shared `useElementSize` rather than growing a second copy. —
  verified in the browser: viewBox now equals the rendered box (`0 0 1502 156` desktop,
  `0 0 348 96` at 390×844), screenshots `.artifacts/planner-shots/n-metrics.png` (before),
  `m-after-dante.png`, `m-phone.png`; the Timeline ribbon still lays out its axis
  (`tl-ribbon.png`). Only one `preserveAspectRatio="none"` existed in `src/**`.

- `5d9aff4` — **Schedule links delivered their query to a page that ignores it.** `/schedule`
  redirects to the Schedule page you last used, carrying the query — right for a bare visit,
  wrong for `?block=` / `?start=` / `?week=` / `?chart=`, which only Calendar and Agenda read.
  Day's own **Schedule** link led back to Day (driven: `/schedule/day` → click Schedule →
  `/schedule/day?start=2026-09-10`); `Schedule block…` on a grid row opened Time Charts or Week
  Plan with no drawer if that was where you had been; the planning wizard's Finish went to the
  same wrong place. Notes had already solved this for its own entry (`?date=` always goes to
  Journal). New `entryPageFor` honours the remembered page only when it can read the link, so
  an Agenda user stays in Agenda and everyone else lands on Calendar; Calendar and Agenda's own
  paging now stays on its own route instead of round-tripping through a `lastPage` the shell
  writes 600ms after arrival. — verified by four `pages.test.ts` cases and in the browser: Day ▸
  Schedule → `/schedule/calendar?start=2026-09-10`; `?start=` after Time Charts → Calendar;
  after Agenda → Agenda; paging Agenda immediately after arriving stays on Agenda. `npm run
smoke` 62/62.

- `b84e467` — **On the phone, the Budget header told you Still needed was "unassigned from
  on-budget accounts".** `readyToAssignNote` describes the big figure, but it was rendered below
  the whole figures row. One line on desktop, so it sat under Ready to Assign by luck; at 390px
  the row wraps, Still needed drops to its own line, and the note landed under it —
  "$2,393.23 Still needed / unassigned from on-budget accounts". It now lives inside the Ready
  to Assign block. — verified at 390×844 (`p-budget.png` before, `p-budget-after.png`) and at
  desktop width, unchanged (`d-budget-after.png`).

- `524f4c0` — **The Register's and Amazon Orders' server-side query parsers were one parser
  written twice.** `ordersQuery.ts` was a line-for-line copy of `registerQuery.ts`'s parsing
  half — search cap, filter allow-list, visible columns, the visible-only sort rule, the
  advanced-filter allow-list, collapsed groups, block offset, filter pass, block slice —
  differing only in the field set. Found by a structural clone scan (function bodies with
  identifiers abstracted), which also showed it is the only real clone family left in
  `src/lib`. Now `lib/grid/serverQuery.ts`, parameterised by the grid's fields; each grid keeps
  only what is its own. −316/+48 in the two modules. — verified by a new `serverQuery.test.ts`
  (the allow-list mutation-checked: dropping the filter guard fails it), both existing suites
  unchanged but for two moved cases, and in the browser (Register search "Wire" → 2 of 7098;
  Orders renders).

- `cee4d1c` — **The time-chart editor's Back button followed any `returnTo`, off-site
  included.** `router.push(returnTo)` took the query parameter as it arrived, so
  `/schedule/time-chart/<id>?returnTo=https://elsewhere` was an open redirect one click from a
  link into your app. The login's `callbackUrl` is the same kind of parameter and was already
  guarded; the editor came later and did not reuse the guard, which lived in `lib/auth` with a
  Plan fallback baked in. The rule is now `lib/navigation/internalPath` and `safeCallbackPath`
  is that plus its fallback. It also stopped a repeated `?returnTo=` (which Next hands over as
  an array) from reaching `router.push`. — verified by `internalPath.test.ts` and the existing
  `callback-url.test.ts`, and driven on the sandbox copy (below): `?returnTo=https://example.com`
  → Back reads "Back to Calendar" and lands on `/schedule/calendar?chart=…`; the app's own
  `returnTo` still reads "Back to Time Charts" and lands there. Other sweeps in the same class came back clean: no other
  URL-supplied destination exists, `UrlLink` already refuses non-http schemes, and
  `react-markdown`'s default URL transform strips `javascript:`.

- `6994376` — **The Bills page's "Still active?" list printed raw date keys** — "Geico ·
  expected 2026-06-26" a few rows below a Next charge column reading 12/26/2026. The 09-05 run
  closed this class across every route, but the list is a collapsed `<details>`, so a page walk
  never saw it. — found and verified by walking all 60 static routes and 35 record drawers
  (every bill, envelope and account, plus payees, contacts, jobs, residences, dated outline
  nodes, a split and a transfer) with every `<details>` forced open, matching visible text for
  ISO day keys. After the fix the only hits are your own note bodies and the Settings
  date-format sample, which shows a key on purpose.

- `e961582` — **On the phone, every Activity row had lost its time, and an unscheduled bill
  its "Unscheduled".** A compact row runs a date column's text through the date formatter,
  which returns `""` for anything that is not a day key — so Activity's Time ("Sep 7, 2026,
  5:08 PM") vanished, and Bills' Next charge, having no compact text of its own, fell back to
  a null key. Compact text now keeps its words, and Next charge gives the phone the desktop
  cell's three answers. — verified at 390×844 on the sandbox, before and after: Activity
  "Budget assignment · Budget" → "Budget assignment · Sep 10, 2026, 10:11 PM · Budget"; Taylor
  Gas "Ungrouped · —" → "Ungrouped · Unscheduled · —". A scan of every column definition found
  these two as the only date columns with compact text.

## Sweeps that found nothing

- **How the second half of the night was run: against a copy of your database.** After the
  preference slip above I stopped clicking in your data. `pg_dump planner | psql
planner_overnight` inside the container, a detached worktree with copied `node_modules`, and
  a second `next dev` on port 3057 pointed at the copy — so assigning, splitting and saving
  could be driven freely. Nothing reaches the real `planner` database from it. Worth keeping:
  it is five commands and it turns "don't touch his data" from a constraint into a non-issue.

- **Every internal link on every static page, and every literal route path in `src/**`.**
  213 distinct hrefs across 60 pages: all 200 or a redirect to a 200. The only 404-ing path
  literals are the three `reserved` modules (`/focus`, `/time-log`, `/reports`), which render
  nowhere by design. Advanced Find's `?detail=` targets open their record on Timeline, Metrics,
  Budget and Payees.

- **Finance invariants on your data, the ones the last two runs did not run.** No allocation on
  an income envelope, none dated off the 1st, no duplicate (month, envelope), no transfer
  carrying a category, no split parent carrying one or missing its children, no stale pending
  row older than 14 days, no income envelope on an outflow, no transaction after its account
  closed, no category or group owned by another user. Two apparent hits were test fixtures:
  the integration suite's users (`finance-mutations-…@localhost`) live in the same database, so
  **scope every data invariant to `test@example.com`'s id** or it reports their deliberate
  half-built rows as yours.
- **Structural clone scan over `src/lib`** (function bodies with identifiers abstracted). One
  real clone family, now folded (above). The component-level hits are deliberate or trivial
  (`daysRemainingTitle` says in its comment it is kept apart from Agenda's on purpose).
- **UTC date keys.** Every `toISOString().slice(0, 10)` left in `src/**` is key arithmetic on
  a `T00:00:00Z`/`T12:00:00Z` anchor or already documented as a deliberate UTC stamp.

## Tried and backed out

- **I wrote one of your preferences while testing, and put it back.** Driving a Register search
  for "Wire" to check the query-parser fold persisted `search: "Wire"` into `grid:finances`,
  which is what the grid is for. Clearing the box leaves `search: ""` — a deliberate override
  that reads as _Unsaved changes_ — so I reloaded the **All Transactions** view through the
  picker, which discards the working copy. The row is byte-identical to before:
  `{"v": 3, "view": "all", "switches": {}, "includeDeferred": true}`. The only other rows
  written tonight are `shell.lastPage` (browsing records where you were — Schedule now remembers
  Calendar, Library Timeline, Finances Register) and `timeline`, back on `grid`, as it was.

## Needs a decision

- **Suggest from Amazon overstates how fast you use things — by 2× for most of what it
  suggests.** The inferred rate is `totalUnits ÷ (lastOrder − firstOrder)`, exactly as
  `specs/2026-08-26-0910-supplies-worksheet/plan.md:295` specifies. That is a fencepost: the
  units bought in the _last_ order have not been used yet — the span measures how long the
  earlier orders lasted. Two orders of one item 30 days apart mean "one lasts 30 days"; the
  formula says 15. Over _N_ equal orders the error is _N_ ÷ (*N*−1). Against your Amazon
  history, **129 of the 217 repeat-purchase suggestions have exactly two orders** (2× too fast)
  and 36 have three (1.5×). Your worksheet shows it: the grooming rake, ordered 8/9 and 8/12,
  reads **"1.5 days each" and $283.88/month**; the honest reading of the same two orders is 3
  days (and really it was an exchange, which no formula will see).

  Options: **(a)** divide by the units bought _before_ the last order —
  `(totalUnits − lastOrderUnits) ÷ span` — one more aggregate in
  `listAmazonRepeatPurchases` and a line in `inferRate`; **(b)** keep the formula and show the
  evidence more loudly, since it is "an estimate the user is expected to correct"; **(c)**
  require three orders before suggesting a rate at all. **Recommendation: (a)** — it is the
  standard inter-purchase estimator, it is right on the case the current one is worst at, and
  it still converges to the same number as orders accumulate. Not taken tonight because the
  frozen spec states the formula in so many words; changing it is a delta-spec, and a
  one-paragraph one. — blocking: nothing; affects only new suggestions, never rows you accepted.

## Noticed but didn't touch

- **Eleven CD-interest rows carry the envelope "Other bills"** — `CD •••2957`, off-budget,
  2023-08 … 2024-06, all last written 2026-08-24 (the category cutover). Harmless: off-budget
  rows are excluded from every envelope sum and the server refuses a new category on one
  (`categoryAssignmentRefusal`). But the value is wrong, and a category on an off-budget row
  should not be representable; nothing clears it if an account moves off budget.
- `src/lib/fitness/sessionDraft.ts:186` — `num()` saves with `Number(raw)`, while
  `parseWeight` (which the +/− buttons and the plate hint use) also accepts a decimal comma.
  Not reachable today: the weight box is `type="number"`, so the browser never hands over a
  comma. Two parsers for one field is the thing to know if that input ever becomes text.

## Gate status at end of run
