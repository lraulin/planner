# Overnight run — 2026-09-09

Started 21:11. Focus: self-directed.

## The short version

**One user-visible defect, and it is yours to decide because the fix picks a heading for every
grid.** Settings ▸ Views & layout tells you that **twelve of your current preferences were
"stored by an older or unrecognized Planner version"** — Budget, its three grids, Accounts,
Bills, Payees, Supplies, the Metrics layout, Advanced Find and Timeline. Only two of the
fourteen rows in that group are actually legacy. Screenshot and the one-line cause are below;
the reason it is not fixed tonight is that fixing it decides which heading each grid sits
under, and that changes the shape of a page you look at.

The rest of the night was **dead surface, and there was a lot of it**: six server actions no
caller reaches — which Next's own docs say are still POST-able — plus nine exports left behind
by reworks that finished, and one dead function whose docstring asserted a requirement that
stopped being true and would have sent the next reader to build the wrong thing. One security
read gained coverage, mutation-tested per half. Five code commits, everything green and
pushed.

**Twelve sweeps found nothing**, including the whole client-side console across 22 pages —
zero errors, zero React warnings — which is precisely the class `npm run smoke` cannot see.

## Shipped

- `b94a0a1` — **Six unreachable server actions deleted.** Every export of a `"use server"`
  module is its own POST endpoint — Next's own `data-security.md:281` says "even if a Server
  Action or utility function is not imported elsewhere in your code, it can still be called
  externally" — so an action nothing calls is a surface with no UI constraining its arguments
  and no test exercising it. All six authenticate through `run`/`runWithData`, so none was a
  hole; they are dead code that happens to be reachable. `listExercisesAction` and
  `listRepeatableTitlesAction` duplicated reads six fitness `page.tsx` files already do
  server-side; `latestSessionByTitleAction` was superseded by `loadLatestForExerciseAction`;
  `setPayeeNotesAction` by `updatePayeeDetailsAction`; `isolatePayeeForBillAction` by
  `trackTransactionAsBill`, which composes the mutation with `upsertBillEnvelope`; and
  `unlinkAccountAction` has no unlink control anywhere in `BankSyncPanel`. Verified by the
  full gate plus `npm run build`.

- `abaaea8` — **A stale claim that would have sent the next reader to build the wrong thing.**
  `flowDiff.ts`'s `summarizeCategoryChanges` had no caller and no test, and its docstring
  asserted "Both fields have to be audited together … Reporting just one would let the other
  half of a regression through" — which reads as a live gap in `npm run flow:audit`. Category
  stopped being a derived column: `previewDerivedChanges` says so in the same file
  ("claims and payee defaults write `budget_category_id` on new or uncategorised rows only"),
  and the only surviving `derivedCategory` is a test-fixture stand-in `analytics.ts:117`
  labels as such. Deleting it also collapsed the indirection it was the reason for —
  `summarize` was generic "because flow and category need exactly the same report", which
  cost two `as PlannedFlowRow` casts and a `read: (row: object)` signature that defeated the
  type checker on the one field that matters — plus `formatFlowDiff`'s unused `label`
  parameter. Verified by the eight existing `flowDiff` tests and by
  `npm run flow:audit`, still 0 of 7100.

- `83b65f4` — **`listAmazonReviewItems` cross-user covered, both halves.** The last Amazon
  read outside `crossUserReads.integration.test.ts`, and it stayed out because the seed had
  nothing flagged: approving the seeded charge's match clears its flag (`apply.ts:469`) and
  nothing flags a subscription on capture, so the assertion would have run against two empty
  tables. The seed now flags one of each — a state the app produces on its own
  (`reconcile.ts:426` flags a matched v1 charge a re-capture supersedes). Mutation-tested
  separately per half, because it is two scoped queries unioned into one list and either
  guard alone keeps the other's rows out: charges guard dropped → `1 failed`, subscriptions
  guard dropped → `1 failed`. Full integration suite green (62 files, 1036 tests).

- `b762762` — **Nine more exports nothing reaches**, each the previous design of something
  that now works differently: `syncGoogleContactsIfStale` (the page's `contactsFreshness`
  makes the staleness call now, without awaiting Google), `googleConnectionState` (a one-line
  alias over `isGoogleLinked`, which the settings panel reads itself), `convertiblePayeeId` /
  `convertibleCategoryId` (the last of the payee-rule cutover — `src/lib/finances/rules/` is
  already gone), `matchKey` (superseded by the occurrence ordinal), `registerBlockAt` (offset
  paging, replaced by id-based `loadRegisterBlock`), `isAssetKind` plus the `ASSET_KINDS` set
  it read, `sourceLabel`, `parseMcpJson`, `isAgentTool`. Verified by typecheck, lint, 4097
  unit tests, 1036 integration tests and `npm run build`; the linter caught two orphaned
  imports and the orphaned `Set`, which is the check working.

## Sweeps that found nothing

Recorded so the next run does not repeat them.

- **Swallowed `catch` blocks** (`clean-code.md` / CLAUDE.md's named anti-pattern). Twenty-nine
  catches in `src/**` are empty or return a bare fallback; every one is a `JSON.parse` /
  `new URL` guard, a best-effort browser API (audio, notifications, `localStorage`), or a
  documented local-mirror rule (`settings/queue.ts:81` — "Anything malformed is dropped rather
  than replayed"). No hole. There is not one `TODO`, `FIXME` or `HACK` in the tree.
- **Client-side console output on 22 pages**, driven in a real browser: outline, tasks,
  projects, goals, chooser, calendar, week-plan, budget, bills, register, insights, accounts,
  payees, supplies, activity, orders, statements, metrics, notes grid, notes journal, contacts,
  resources, timeline, jobs, residences, fitness sessions, exercises, find, agenda, day.
  **Zero errors and zero React warnings** — the whole console is the devtools banner and HMR.
  This is the class `npm run smoke` structurally cannot see, since it only asks whether the
  route renders server-side.
- **`npm run smoke`** — all 62 routes rendered.
- **Bill-envelope invariants as SQL against your real data.** Cancelled status and
  `cancelled_on` agree in both directions; no bill with neither cadence or with both; no
  `lead_days` without a `due_day`; `due_day` in 1–31 and `lead_days` in 0–60 everywhere; no
  non-bill carrying a bill-only column.
- **`user_settings` scope validity.** All 36 of your stored scopes parse; every scope kind the
  app constructs is in `SCOPE_KINDS`, so no preference write is silently rejected. Grid tab ids
  all satisfy `KEY_PATTERN`. `SETTINGS_VERSION` is written but read only by
  `parseGridSettings`, exactly as `scopes.ts` says.
- **Month-end date arithmetic where a day number becomes a date.** `dueInMonth` clamps a due
  day into a short month with the same rule `shiftDateKeyMonths` uses, and says so; the target
  engine never turns a day number into a date at all.
- **"A routine can never read as Overdue."** Holds, and for the reason the docstring gives:
  `moveDates` only ever _advances_ a deadline, never invents one, and `scheduleStatus` cannot
  reach `overdue` without one.
- **Days remaining on the Bills page, against today.** Geico 12/26 → 108, SMECO 9/30 → 21,
  Rent 9/24 → 15: all exact. `Due soon` is correctly empty, and both it and the column read
  the same `nextDueKey`, so the 14-day window and the number cannot disagree.
- **The stored contact/node item kinds against the ones the drawer renders.** Nothing stored is
  invisible.
- **Every write path into a `target` JSONB.** `saveEnvelopeTarget` is the only one and it goes
  through `parseTargetOrThrow`, so a malformed target throws rather than being stored and then
  silently vanishing on the next read.

## Tried and backed out

- **The dead-export sweep, stopped one third short.** After the two deletion commits, about a
  dozen unreferenced one-liners are left and I left them: `readZoomParam` and
  `notesJournalPath` are each one of a symmetric family (`readDetailParam` / `readViewParam` /
  `readNoteParam`, and `notesPath`), `otherPromotionCents` is half of a documented pair whose
  other half is live, `isSubmenu` sits with two private siblings that do the same job. Deleting
  one member of a symmetric set makes the module read worse than the dead line does. Listed
  under **Noticed** instead.

- **`registerBlockAt`'s neighbour `parseBlockOffset`, deleted and put straight back.** My
  cross-file scan reported it as referenced only by its own test, which is true and misleading:
  `sliceRegisterBlock` calls it in the same file, twenty lines above. Typecheck caught it
  immediately. Same trap as the coverage scans last night — _a scan that answers a narrower
  question than the one you are asking still returns an answer._ The cross-file scan is for
  "who imports this"; only the version that also counts same-file uses answers "is this dead".

- **Direct tests for `yamlMapping` / `yamlSequenceItem`.** They looked untested (no test file
  mentions either name) and a mutation confirmed one branch is uncovered: `yamlSequenceItem`
  returning `- {}` for an empty map survives being replaced with `""`. But no caller can reach
  it — `budget/export.ts` and `audit/export.ts` both pass populated sections, and both assert
  their whole document. A test for an unreachable branch is the kind `testing.md` says does not
  earn its place.

- **Coverage-guided test selection generally.** No coverage provider is installed and adding
  one is a dependency. The by-hand substitute — exported names no test file mentions — is
  mostly false positives here: `parseFilterNumber` looked untested and is covered thoroughly
  through `matchesCondition` (accounting negatives, `$`, thousands separators, blank operand,
  fail-closed on both sides). This codebase tests through its entry points, so a name-based
  coverage scan reads it wrong in both directions.

## Needs a decision

- **Settings ▸ Preferences tells you that 12 of your current preferences were "stored by an
  older or unrecognized Planner version".** Not a label nobody sees:
  `SettingsPage.tsx:418` prints that sentence under every row in the **Other** group, and
  `buildPreferenceGroups` run against your real `user_settings` puts **14 of your 36 rows**
  there — of which only two are actually legacy.

  ```
  ## Other (other) — 14
     budget                          | Budget setup       ← live (Budget start month / opening)
     find                            | Advanced Find      ← live
     grid:budget                     | Grid — Budget      ← live
     grid:budget-bills               | ...                ← live
     grid:budget-envelopes           | ...                ← live
     grid:budget-savings             | ...                ← live
     grid:finance-accounts           | ...                ← live
     grid:finance-bills              | ...                ← live
     grid:finance-payees             | ...                ← live
     grid:metrics-layout             | ...                ← live
     grid:supplies                   | ...                ← live
     timeline                        | Timeline           ← live
     grid:finance-commitments-bills  | ...                ← genuinely legacy
     grid:finance-commitments-spend  | ...                ← genuinely legacy
  ```

  Confirmed in the running app, not only in the function: **Settings ▸ Views & layout**
  renders fourteen rows carrying that sentence, under a heading reading **Other** with its own
  **Reset module** link. Screenshot: `.artifacts/planner-shots/settings-other.png`.

  The cause is one line. `moduleEntry` (`src/lib/settings/management.ts:87`) takes a grid
  scope's key, cuts it at the first `.`, and requires the remainder to be a key of
  `MODULE_LABELS` — a fifteen-entry list from before the module/page split. Every grid tab id
  added since (`budget-bills`, `finance-accounts`, `supplies`, `metrics-layout`, …) and every
  scope kind with no arm in that function (`budget`, `find`, `timeline`) falls through to the
  `other` bucket, whose `detail` is hard-coded to that sentence.

  Two consequences beyond the wrong words: resetting **Finances** does not reset any finance
  grid (its `resetScopes` is built from the entries it grouped), and every future grid lands in
  Other silently. The unit test does not catch it because its fixture's only Other case is a
  made-up `retired:layout` — no live-but-unlisted grid id
  (`management.test.ts:40`).

  There are three namespaces here and none of them agree: **module** ids
  (`plan`, `finances`, `library`, … — `components/shell/modules.ts`), **page** ids
  (`budget`, `accounts`, `payees`, … — `lib/navigation/pages.ts`), and **grid tab ids**
  (`finance-accounts`, `budget-bills`, `supplies`), which are their own namespace with a
  `finance-` prefix applied inconsistently. `management.ts` assumes the third equals the first.

  Options: **(a)** one registry in `src/lib` mapping every grid tab id to the page it belongs
  to, with a unit test asserting every id passed to `useGridState` / `useModuleViews` appears
  in it, and headings taken from `pages.ts`'s own labels; **(b)** rename the grid tab ids to
  match page ids — `modules.ts` already rejects this in writing ("renaming a persisted scope
  key would be a migration bought for nothing"), and it would orphan every stored row;
  **(c)** leave the grouping and only fix the sentence, so a row with a known scope kind stays
  in Other with an honest detail line.

  **Recommendation: (a), with (c) shippable tonight-cheap if you want the lie gone first.**
  (a) is the only one that makes the _next_ grid a test failure instead of another silent
  Other row, and it needs no migration. Not taken tonight because it decides which heading
  each grid sits under — "Budget bills" under **Budget** or under **Finances** is your call,
  and it changes the shape of a settings page you look at.
  — blocking: nothing. The two genuinely legacy `finance-commitments-*` rows are already
  resettable from that page; I did not touch your data.

## Noticed but didn't touch

- **Two orphan `user_settings` rows in your real data**: `grid:finance-commitments-bills` and
  `grid:finance-commitments-spend`, from the Commitments grids that the one-budget work retired
  (migration `0072_drop_commitment_tables.sql`). Harmless, and already reachable from
  Settings ▸ Views & layout ▸ Other, where a **Reset** button removes each. I did not touch
  your data.

- `src/db/schema.ts:2627` — `financeReportingArchive` is a table **nothing reads and nothing
  writes**; its own docstring says so ("Historical exclusion metadata retired by envelope
  reporting. No product writes."). Worth knowing _why_ the declaration stays: removing it from
  `schema.ts` is what would make the next `db:generate` emit a `DROP TABLE`. The declaration is
  the only thing keeping the archive alive, which is a load-bearing fact that reads like dead
  code.

- `src/lib/amazon/mutations.ts:75` — `deleteAmazonChargeMatch` has no caller and there is no
  unmatch control anywhere in the Amazon UI. Wire it or delete it; unlike tonight's deletions
  it has no superseding path to point at, so it is a small product question rather than
  housekeeping.

- Still unreferenced after tonight, and left deliberately (see **Tried and backed out**):
  `readZoomParam`, `notesJournalPath`, `otherPromotionCents`, `isSubmenu`, `itemsInDayLetter`,
  `EMPTY_REPORT_FILTERS`, `defaultExercisePrefs`, `OLYMPIC_BAR_LB` / `OLYMPIC_BAR_KG`,
  `isCheckedAppearance`, `DEFAULT_DISPLAY_SETTINGS`, `STATEMENT_COLUMN_IDS`.
  `DEFAULT_DISPLAY_SETTINGS` is the odd one: every sibling settings module exports a
  `DEFAULT_*` its parser uses, and `parseDisplaySettings` inlines `DEFAULT_DATE_FORMAT`
  instead — a one-line consistency fix if the pattern is meant to hold.

- **The `useGridState` tab ids are a third namespace** beside module ids and page ids, with a
  `finance-` prefix applied to four grids and not to `budget-bills`, `supplies`,
  `insights-envelopes` or `metric-tracking`. Nothing depends on the prefix; it is what made the
  settings grouping above look like it could work. If the registry in that recommendation gets
  built, it is the place to write the convention down.

- **The machine was under load average 38 for part of the night** — Codex, VS Code, a Grok
  helper and Playwright runs in two other repos, none of them mine. It turned a 3.8s unit suite
  into a 1046s one with seven vitest worker RPC timeouts inside a pre-commit hook. Nothing was
  wrong with the code; the same suite passed in 3.8s minutes before and after. Worth knowing
  the hook has no timeout of its own to tell those apart.

## Gate status at end of run

(pending)
