# Overnight run — 2026-08-06

Started 21:06. Focus: self-directed.

Baseline before touching anything, on `75a67f1` with Postgres up: typecheck, lint and the
full suite green — **132 files, 1945 tests**, no Postgres skip warning, so the 19
integration files really ran.

## Shipped

- `db54dc7` — **Select the rows the grid is showing, not the ones handed to it.** Six views
  — Notes, Day, Wish List, Contacts, Resources, Time Charts — built `useMultiSelect`'s
  order from the rows they passed *into* `DataGrid`, but the grid owns the narrowing
  (`DataGrid.tsx:426-490`: filters, quick search, advanced filter, multi-column sort,
  collapsed groups). The other six were fixed by `onNavigableIdsChange` in the right-click
  slice; these were missed. Reproduced and then re-broken to be sure: search `/notes` for
  `2014-07-31`, click `2014`, Shift+Down twice — the toolbar reads **`3 selected`** and
  offers **`Delete (3)`** while exactly one row is highlighted, because the other two are
  the next rows in the *unfiltered* tree. On Notes that Delete removes two notes the user
  cannot see; on Day the same list drives Rank A–D, the Mark states and Move to tomorrow
  (`DailyItemsGrid.tsx:217`). Fixed by `useNavigableIds`, which also keeps the fallback
  array memoised. Verified in the app both ways —
  `.artifacts/planner-shots/notes-shift-range.png` shows the three highlighted rows now
  matching the three the count claims. Reproduced independently on a second host later in
  the night: search `/wishes` for `s`, leaving *heavy weights · stall bars · Stoicism*, and
  Shift+Down from the first row selects `punching bag` — filtered out, off screen, and
  invisibly part of anything the selection then acts on. With the fix it selects
  `stall bars`, the row underneath.

- `ff6da72` — **Walk the Metrics rows in the order they are drawn.** `navigableIds` came
  from `visible` (pre-grouping) while both the table and the compact list render `grouped`,
  which hoists the ownerless group to the front and then goes alphabetically by owner —
  and `sortKey` interleaves owners. Reproduced with one metric owned by a goal: the table
  drew Dante Weight / New Metric / Bodyweight, and on Bodyweight (the bottom row) ArrowUp
  left it where it was while ArrowDown jumped to Dante Weight at the top. Confirmed both
  ways by reverting the source and re-running the same steps.

- `177471f` — **Remember how the Metrics tab is set up.** All five switches — Active only,
  Group by Owner, Show Performance, Show Legend, Show Objective — were component
  `useState`, so `/metrics` reset them on every visit: turn off Active only to find a
  retired metric, go to the Outline and back, and it is hidden again. Metrics is the module
  with no `GridToolbar`, which is how it missed the rule the persistent-UI-state slice set
  for every other tab. Stored in the `grid:metrics-layout` scope the pane height already
  uses, since `parseMetricsLayout` falls back per key and a blob holding only a height
  keeps it. Four new codec cases, including false-is-not-absent and the pre-upgrade blob.
  Verified in the app — `.artifacts/planner-shots/metrics-persisted.png`, Active only off
  and Group by Owner on after a hard reload.

- `de0c53f` — **Remember how the Projects rail is filtered.** Show Completed, Group by
  Result Area, Show Tasks and Sort by Priority were also `useState`: turn on Show Tasks so
  a single task can be dragged onto the week, leave the page, come back, and the rail is
  projects-only again. Into the existing `schedule` scope beside slot granularity and Work
  Week Mode. `SCHEDULE_VIEW_CODEC` moved out of `ScheduleView` into its own module, because
  `useSetting` memoises on the codec's identity and two components now read it. Verified:
  Show Tasks and Sort by Priority survived a hard reload while the other two stayed off, so
  it is not writing the whole scope —
  `.artifacts/planner-shots/schedule-rail.png`.

- `5bbef5b` — **Build the rail's node index once, not once per row.** `areaName` rebuilt
  the whole `id -> node` map inside itself on every call, so grouping cost one map per row
  grouped. Found while wiring the switch above.

- `73344cf` — **Keep the Task Chooser's date filter with the view it belongs to.** The Date
  dropdown — Achieve's nine bands, manual §8 — was `useState("none")` and was never in
  `ChooserSettings` at all, though every other Chooser control has been per-view in
  `chooser:{viewId}` since the views-across-modules slice. Per view rather than
  module-wide, matching the rest of that blob: Urgent narrowed to Overdue while Best
  Overall shows everything is a coherent pair, and a shared value would make picking a view
  silently re-narrow the list. Verified in the app: Overdue took the list from 20 rows to 2,
  survived a hard reload, and switching to Urgent showed `None` while Best Overall kept
  `Overdue`. The parse guard earns its keep — `applyDateFilter`'s switch has no default
  branch, so an unrecognised id returns `undefined` from the predicate and empties the grid;
  `asOneOf` against the dropdown's own list turns a renamed band into "no filter".

- `d02d0a0` — **Write down the two rules six views broke tonight.** `data-grid.md` already
  forbade a host deriving the navigable list itself, but it listed only the filters and the
  search — not **sort**, which is the case that catches people out, because it removes
  nothing and only the order is wrong. It also never said what the consequence is. And its
  persistence rule was phrased "through `useGridState`", which is exactly why the three
  modules without a `GridToolbar` read it as not applying to them. Both now say what they
  mean, plus `useNavigableIds` and the per-key-fallback rule for adding a key to a live
  scope. `index.yml` updated to match.

- `70c3fac` — **Compare the agent key without leaking how much of it matched.**
  `requireAgentApiKey` used `token !== expected`, which returns at the first differing byte.
  It guards a write surface — `/api/agent/*` creates and deletes nodes, and Alfred and the
  Reminders drain already post to it — and the roadmap points the same key at a public HTTPS
  endpoint for remote MCP. Both sides are now SHA-256'd before `timingSafeEqual` (which wants
  equal lengths and throws otherwise), so the key's length leaks nothing either. Verified
  over real HTTP: the configured 64-char key returns 200 from `health` and real data from
  `search_nodes`; a wrong key, a five-character prefix of the right one, and no header all
  return 401.

- `8ceb171` — **One cross-user check across every read path.** I set out believing the query
  layer had no cross-user coverage. That was wrong and worth saying: most mutation files
  already carry a block that reaches their queries. So I measured instead — dropped
  `eq(userId)` from four read paths and ran the suite without the new file. Resources and
  time charts were caught. **`getMetricEntry` and `loadWishList` were not, and with both
  returning every user's rows all 1956 existing tests passed.** `loadWishList` reads
  `node_items` directly and is the entire `/wishes` module. The new file covers all sixteen
  read surfaces in one list, and its first case asserts the owner *does* see everything —
  without that, every other assertion would also pass against an empty database. Confirmed
  it fails on the mistake it exists for by sabotaging notes, planning and fitness in turn.

## Tried and backed out

- **Nothing was reverted, and no cycle went sideways.** Two suspicions were chased to a
  conclusion and turned out to be no bug, which is the outcome the brief asks for rather
  than a fix on suspicion:

- **Paste order under concurrent server actions — chased, then settled, no bug.**
  `useNodeCommandDeck.onPasteRows` (`:123`) computes the moves up front and fires one
  `apply(...)` per row, each its own `startTransition`; every move after the first is
  anchored to the previous row, so `pasteMoves`' promise that "a block picked up in order
  arrives in that order" only holds if they are applied in order. I could not settle from
  reading whether Next.js serialises Server Action dispatches across separate transitions,
  so I measured it instead of guessing or of moving Lee's real rows: four throwaway tasks
  posted through `/api/agent/capture`, cut three of them on `/outline`, pasted after the
  fourth. Result **zzTarget · zzA · zzB · zzC** — right order, confirmed after a reload and
  again straight from the server through `search_nodes`, so it is not just optimistic UI.
  The four rows were then deleted and the outline is back to its original 73.

- **Clicking an Outline row "not selecting".** Looked like a live bug for a while: `click`
  on a row below the fold left the selection where it was. It is the driver — the rows
  live in a scroll container, and `document.elementFromPoint` at the row's centre returns
  `null`, so the click lands on nothing. Not an app defect; noted so the next run does not
  chase it again.

- **Independently verified, no defect found — recorded so the next run does not redo it.**
  I spent the last stretch checking the claims of the frozen `2026-08-06-1506` spec rather
  than trusting them, since it was written and frozen the same day:

  - The **calendar right-click** resolves both an empty slot and its date by hit-testing, and
    its `Slot size ▸` fly-out opens, greys the value already in force, and greys
    `Week of this day` when you are already on that week
    (`.artifacts/planner-shots/calendar-submenu.png`). Slot granularity really persists —
    48 lanes → 96 at 15 minutes → still 96 after a hard reload. Restored to 30 afterwards.
  - **`Schedule block…`** from a task row lands on `/schedule`, and the drawer is genuinely
    prefilled: subject `Do bench press`, `23:00`–`23:30` — a 30-minute span matching the
    task's own effort — and the PROJECT select already holding the owning project rather
    than `(None)`.
  - The **completion cascade confirmation** exists and says the right thing: *"Complete
    'Become a Programmer/Web Developer' and what is under it? 7 open items underneath will
    also be marked Completed. Re-opening this one later will not re-open them."* It briefly
    looked like a no-op because it is `role="alertdialog"`, not `role="dialog"`. Cancelled;
    the project is still `in_progress`, checked through the agent API.
  - The **row menu is a real bottom sheet** below `md`: a synthesised 900 ms long-press on
    `/notes` at 390×844 opens a panel at `0,368` measuring `390×476` — full width, flush to
    the bottom edge, ignoring a press point that was at `y=245`
    (`.artifacts/planner-shots/mobile-row-sheet.png`).
  - **`?scope=`** round-trips: `View tasks…` navigates to `/tasks?scope=<projectId>`, narrows
    to that project's two tasks, and a cold load of the same URL reproduces it.

## Needs a decision

- **Editing an appointment whose Google event was deleted elsewhere fails, permanently.**
  `pushUpdate` (`src/lib/google/writeThrough.ts:92`) patches Google inside the mutation and
  does not catch `GoogleEventGoneError`, which `googleFetch` throws on 404/410
  (`client.ts:115`). `pushDelete` right below it *does* catch it, deliberately. So: delete
  an event on your phone, edit the still-mirrored row here before the next mirror pass, and
  every save throws. Options: (a) leave it — the asymmetry is defensible, since a patch that
  cannot reach Google must not quietly succeed locally, and a reload drops the row;
  (b) catch `GoogleEventGoneError` in `pushUpdate` and delete the local row instead, on the
  grounds that Google is the source of truth and the row is provably stale; (c) catch it and
  surface "this event no longer exists in Google — refresh". **Recommendation:** (c). It is
  the honest message, and (b) throws away planner-only fields (three-state check, priority,
  contexts, project link) on what may be a transient 404.
  — blocking: nothing today; needs a linked account to hit at all.

- **A completed parent with an open, overdue child reads as Overdue.**
  `scheduleStatusById` (`src/lib/tree/status.ts:222`) propagates a child's `overdue` up, and
  `moreUrgent` ranks `overdue` above `completed` — so the row's State says Completed while
  its Status says Overdue. Reachable by completing a project and later reopening a child, or
  filing a new task under a finished one. Options: (a) stop propagation into a parent whose
  own local status is `completed`; (b) leave it, on the grounds that "marked done but has
  overdue work under it" is worth shouting about. **Recommendation:** (b) plus nothing —
  I think the current behaviour is right and the contradiction is the point, but it is a
  product call and I did not want to change a status rule unattended.
  — blocking: nothing.

- **The rail groups in insertion order; Metrics sorts its groups.** `ProjectsRail`'s Group
  by Result Area lists areas in whatever order the projects happen to come in, with
  `(No Result Area)` wherever it first appears; `MetricsView` sorts owner groups
  alphabetically with the ownerless one pinned first. One of the two is wrong and it is a
  taste call which. **Recommendation:** match Metrics — pinned "none" then alphabetical — it
  is the one a reader can predict.
  — blocking: nothing.

## Noticed but didn't touch

- `src/components/fitness/SessionEditor.tsx:131` — the `exhaustive-deps` suppression keys
  the draft on `existing?.id` but closes over `catalogProp`. If the exercise catalog changes
  while the editor is open the draft is built from a stale one. Narrow enough that I left it.

- `src/lib/metrics/queries.ts:48` — `listMetrics` left-joins `nodes` on `metrics.ownerNodeId`
  without scoping the join to the user, and `getMetricDetail:155` does the same. Checked
  before writing this down: it is **not** exploitable, because `assertOwnerOk`
  (`metrics/mutations.ts:51`) is the only way `ownerNodeId` is ever set and it requires the
  node to be the caller's. Still the one pair of joins in the query layer that does not carry
  `userId`, so it depends on a guard two files away rather than on the query.

- **Audited and clean, so nothing to do — recorded so the next run does not redo it:** every
  server action in `src/app/**` resolves the user itself (the six that looked unguarded go
  through a `runQuery` wrapper); every agent tool in `dispatchAgentTool` takes an explicit
  `uid`; `upsertPlanEntry` scopes both the plan and the node; `deleteEvent` already tolerates
  `GoogleEventGoneError`; `devAuthBypassEnabled` has both an inlined `NODE_ENV` gate and an
  opt-in env var. No console errors and no horizontal overflow on any of the sixteen modules.

- `src/lib/tree/completionCascade.ts:75` — `settleDescendants` walks children with no cycle
  guard while `reopenAncestors:117` right below it has one. Unreachable (`moveNode` refuses
  cycles), so this is asymmetry rather than a defect.

- `ProjectsRail` groups by insertion order — see _Needs a decision_. Not touched because
  which of the two orderings is right is a taste call.

## Gate status at end of run

Re-run at the end of the night on `523fe48`, dev server stopped, Postgres up:

| Check                      | Result                                           |
| -------------------------- | ------------------------------------------------ |
| `npm run typecheck`        | pass                                             |
| `npm run lint`             | pass (0 errors, 0 warnings — `--max-warnings=0`) |
| `npm test`                 | pass — **133 files, 1968 tests**                 |
| `npm run test:integration` | pass — **20 files, 454 tests**                   |
| `npm run build`            | pass                                             |

**The database tests really ran.** Postgres (`planner-postgres`) was up for the whole run;
every integration file executed and the skip warning never appeared — grepped for, count
zero. The suite went 1945 → 1968 over the night; integration files 19 → 20 with the new
cross-user read file.

Working tree clean, everything pushed to `origin/master`. Nine commits of work on top of
`75a67f1`, plus five that are this log:

|           |                                                                 |
| --------- | --------------------------------------------------------------- |
| `db54dc7` | Select the rows the grid is showing, not the ones handed to it   |
| `ff6da72` | Walk the Metrics rows in the order they are drawn                |
| `177471f` | Remember how the Metrics tab is set up                           |
| `de0c53f` | Remember how the Projects rail is filtered                       |
| `5bbef5b` | Build the rail's node index once, not once per row               |
| `73344cf` | Keep the Task Chooser's date filter with the view it belongs to  |
| `d02d0a0` | Write down the two rules six views broke tonight                 |
| `70c3fac` | Compare the agent key without leaking how much of it matched     |
| `8ceb171` | Put one cross-user check across every read path                  |

Browser verification at 1280×800 and 390×844 — `.artifacts/planner-shots/`:
`notes-shift-range.png`, `metrics-persisted.png`, `metrics-grouped.png`,
`schedule-rail.png`, `chooser-date-filter.png`, `calendar-submenu.png`,
`mobile-row-sheet.png`. **No console errors** on any of the sixteen
modules, and **no horizontal page overflow** at 390px on any of them (measured as
`scrollWidth - innerWidth`, zero everywhere).

**Dev database:** one metric was created and deleted again, and `Bodyweight`'s owner was set
to a goal and set back to null, both to reproduce the Metrics grouping bug. Confirmed by
timestamp that the two other stray `New Metric` / `test` rows date from 2026-08-02 and are
not mine, so they were left alone. The rail's Show Tasks / Sort by Priority and the Chooser's
date filter were toggled back to their defaults after verification. Every sabotage used to
prove a test earns its place was reverted with `git checkout --` and the tree re-verified
clean each time. Four throwaway tasks created through `/api/agent/capture` for the
paste-order test were deleted afterwards and the outline is back to its original 73 rows;
the Notes quick search and the calendar's slot size were both put back where I found them.
