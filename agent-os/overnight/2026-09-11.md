# Overnight run — 2026-09-11

Started 21:38. Focus: self-directed.

## Shipped

- `def47495` — **the integration suite ran on vitest's 5s default timeout**, which is a budget
  for the assertion and not for the machine. The 09-09 run, with the box held at load 20-40,
  timed a 600ms test out at 7.9s and failed the pre-push hook three times on three different
  tests, none of them wrong — a green gate you cannot trust is worse than a slow one. Now 30s a
  test and 30s a hook, on the integration project only: the unit suite is hermetic and has no
  reason to wait. The slowest real integration test is **851ms** with the box idle, so the new
  ceiling is ~35× it and still bounds a genuine hang. Verified rather than assumed, because the
  config file warns that `isolate` and `poolOptions.forks.*` are accepted and **silently
  ignored** on a project: a probe sleeping 6.5s passed at **6502ms** under the integration
  project and would have failed at the old 5s.

- `1d8fd3df` — **a typo in the Orders grid's visible-column list was not a compile error.**
  `AMAZON_VISIBLE_COLUMN_IDS` was `as const` with nothing tying it to `AmazonFieldId`, though it
  feeds both `AMAZON_COLUMN_IDS` and `ordersQuery`'s `defaultVisible` — so a stray or retired id
  would have produced a default-visible column with no accessor behind it rather than failing to
  build. `satisfies readonly AmazonFieldId[]` keeps the literal tuple type every reader relies on
  while requiring each entry to be a real field. It typechecks unchanged, so the invariant held
  today; this only stops the next edit from breaking it quietly.

- `bc84ca68` — **`learn.ts` had no test of any kind**, and it is what decides the Category a
  merchant's future charges get. `autoCategory.test.ts` covers the decisions it delegates to;
  what was untested is the half `learn.ts` owns — building the history those decisions read,
  which is a join against accounts plus a `categoryAssignableIds` filter, ordered by date. Both
  are places where a mistake returns a plausible wrong default rather than an error. Nine cases
  now, and rather than assume they bite, I broke the code twice and watched exactly one test
  fail each time: **reversing the sort** fails only _"reads the latest three by date"_ (it uses
  five charges precisely so the newest three and the oldest three disagree), and **dropping the
  off-budget exclusion** fails only _"treats an off-budget charge as neither evidence nor a
  reason to wait"_ — where an uncategorised brokerage row would otherwise hold the first default
  back forever, since it can never be given a Category at all. Also covers the internal-transfer
  exclusion, the edit-outside-the-window no-op, relearn's claimed and fixed refusals, and the
  second-user check the testing standard requires.

## Sweeps that found nothing

- **No missing `userId` scoping anywhere in the mutation/query surface.** Scanned all 540
  `.delete(` / `.update(` / `.select(` statements across the 50 `mutations.ts` / `queries.ts`
  files; 22 came back without the string `userId` and every one of them is scoped in a way the
  grep could not see. Three kinds, worth writing down so the next run does not redo it:
  - **Detail-table writes riding on a verified node.** `taskDetails`, `goalDetails`,
    `projectDetails` and `resultAreaDetails` are keyed 1:1 off `nodeId`, and in every case the
    sibling `nodes` write in the same function carries `eq(nodes.userId, userId)` — or the ids
    came from `subtreeIds(tx, userId, nodeId)` (`tree/mutations.ts:977`) or from an `InboxItem`
    already fetched for the user (`organizer/mutations.ts:223`).
  - **The userId is behind a variable.** `find/queries.ts:316,329,345` pass `.where(scoped)`;
    `contacts/mutations.ts:196` spreads a `where` array whose first element is
    `eq(contactItems.userId, userId)` (`:188`).
  - **The `.where` was past my capture window**, not absent — `finances/budget/mutations.ts:863`
    reaches its `eq(financeBudgetCategories.userId, userId)` 39 lines below the `.update(`, and
    `finances/queries.ts:536` likewise.

  The scan script is at `scratchpad/scan-scoping.mjs` in this session's scratchpad; it is the
  count that lies, so any rerun should read the hits.

- **The finance specs' D-lists, read as a checklist against the code — 09-09 recommended this
  and it is now done for the September finance folders.** Six claims that name a symbol or a
  hard rule, all six holding:
  - `bills-days-remaining` **D1/D6** — the Register strip is _gone_, not hidden:
    `upcomingBillOccurrences`, `initialUpcoming`, `upcomingBillsAction` and `loadUpcomingBills`
    return zero hits in `src/**`. Worth naming because D6's conditional deletion ("if nothing
    else calls it") was **not** among the 22 symbols 09-09 swept.
  - **D5** — `billDaysRemaining` is in `budget/dueCue.ts:72` and counts with `daysBetweenKeys`,
    not a `Date` subtraction, returning null for cancelled/unscheduled/no-charge as D2 and D3 ask.
  - `bill-due-dates-and-lead-time` **D3** — the grace floor is `Math.max(7, …)`
    (`commitments.ts:281`), and its docstring records the replay the 7 was measured from.
  - `cancelled-bill-handling` **D6** — `loadBillAnchors` skips on `walksNextDue`
    (`inspector.ts:136` = `scheduled && status !== "cancelled"`), and `loadNextDueKeys`
    delegates to it, so the two cannot drift apart.
  - `still-needed-this-month` **D1/D2** — the clamp is per envelope: `gapOf` takes
    `max(0, needed − assigned)` per row _before_ the sum, so an overassigned envelope
    contributes `0` instead of offsetting an underfunded one. `underfundedGapCents` is literally
    `stillNeeded(...).totalCents`, so the headline and the pills cannot grow a second opinion.
  - `move-to-ready-to-assign` **D4** — the committed id is the sentinel
    `READY_TO_ASSIGN_DESTINATION = "__ready_to_assign__"` (`groupEnvelopeOptions.ts:109`), not a
    UUID and not a category.

  The finance/budget area has now been swept three nights running and is dry; the specs whose
  headings this grep does **not** match (fitness, outline, grids — they number decisions
  differently) are the unswept ones if a future run wants this detector.

- **"Which modules have no test?" — asked four different ways, wrong all four times.** I went
  looking for untested pure logic and the file-level heuristics kept overstating it. Worth
  recording as a method note, because each lie had a different cause:
  - `src/lib/**` modules with no sibling `foo.test.ts`: **162**. But `mutations.ts` and
    `queries.ts` are tested by `foo.integration.test.ts`, a different filename. Real figure: 117.
  - Grepping test files for a module's **basename** put `day/sync`, `banksync/sync` and
    `google/sync` at "90 test files" each — the word `sync` appears in 90 tests.
  - `url/extractHttpUrls.ts`: **0** tests import it, and it is thoroughly tested. `taskNameLinks.ts`
    re-exports both its functions and `taskNameLinks.test.ts` drives them through that — Wikipedia
    parens, markdown brackets, bare hosts, `v1.2` rejection, the lot. I had a test file half
    designed before I checked; it would have restated coverage that already existed.
  - `src/lib/amazon/mutations.ts` is the **only** `mutations.ts` in the tree with no sibling
    integration test — and it is covered, by `db/crossUserReads`, `amazon/evidence` and
    `amazon/import`. Its own docstring says so ("Used by isolation tests"). All six functions
    carry `eq(table.userId, userId)`; nothing is unscoped. (`deleteAmazonChargeMatch` still has
    no caller, as 09-09 noted — unchanged, and still a product question rather than housekeeping.)

  The generalisation: **a filename is not coverage and a grep count is not a fact.** Every one of
  these was settled by reading a single hit.

- **The cross-user read suite has no hole in it.** `src/lib/db/crossUserReads.integration.test.ts`
  is 1143 lines driving a **hand-written** list of 174 imported functions, so its coverage is
  only as good as that list — and nothing makes the list grow. Diffed it against every exported
  function in every `queries.ts`: **15 are not in it.** All 15 take a `userId`, and reading the
  six with no integration coverage anywhere settles them — every one is a _composer_, not a query
  author. `loadAmazonPrepared` and `loadAmazonExportRows` go through `listAmazonItems(userId)`,
  `loadTrackAsBillDraft` through `listTransactions(userId)`, `loadNoteIdsMatchingFilter` through
  `loadNotes(userId)`, `loadWeeklyPlanPayload` through seven loaders each passed `userId`, and
  `loadReviewCandidates` through two covered loaders plus one direct query scoped
  `eq(financePayees.userId, userId)`. Not one issues an unscoped query, and every function they
  delegate to is already in the suite.

## Tried and backed out

_(nothing yet)_

## Still waiting on you, carried from earlier runs

Nothing below was taken tonight: each changes a rule a frozen spec states in so many words,
which is Lee's call, not an unattended one. Full argument and options in each night's report.

- **Fitness ▸ Start last will log your next Push into August 31** — resumes any session with an
  unchecked set, whatever its age. Worth deciding before your next gym session; **Empty workout**
  avoids it meanwhile. (`2026-09-10.md`, _Needs a decision_.)
- **SMECO's bill claim refuses five of every six of its own charges**, which also keeps its
  September budget silent. (`2026-09-10.md`.)
- **Discover recurring charges shows a yearly figure its own cadence cannot produce** — Walmart
  at $9,877.72/yr against a Monthly cadence worth $2,271.72. (`2026-09-10.md`.)
- **Suggest from Amazon overstates usage ~2×** for the 129 of 217 suggestions with exactly two
  orders. (`2026-09-10.md`.)
- **Settings ▸ Views & layout calls 12 live preferences "stored by an older or unrecognized
  Planner version"** — needs your call on which heading each grid sits under.
  (`2026-09-09.md`.)
- **Capital One register dates changed meaning on 2026-08-11** when SimpleFIN took over.
  (`2026-09-09.md`.)

## Noticed but didn't touch

- **You have a spec that has been finished for twelve days and is still marked active —
  `2026-08-30-1331-category-picker-everywhere`.** Both `plan.md` and `shape.md` say
  **Status: active**, all seven acceptance boxes are unticked, and its own change table says
  Tasks 2–6 shipped. I went through it claim by claim tonight and **the code does everything the
  spec asks.** `moveTargets` is `rows.filter((row) => row.id !== sourceId && !row.isIncome)` —
  one line covering both "source omitted" and "no Income" for Move money. Assign is handed
  `[...envelopes, ...bills, ...savings]`, which is where its "no Income" comes from. Both dialogs
  build through `visibleEnvelopeCatalog` with `allowClear={false}` and no `onCreate`; Supplies'
  `fundingCatalog` drops income and keeps hidden; Payees keeps both and stays clearable to
  "None"; Register, drawer, splits and the grid cell still pass `onCreate`. The unit-test box is
  satisfied down to its wording — there is a `describe("categoryPickerSections — includeCreate:
false")`, and `allowClear: false` is covered as `commitCategoryPicker`'s fourth parameter,
  which returns `restore` on an empty draft.

  I ticked **only** the unit-test box and wrote the evidence into `plan.md`. Boxes 1–5 each pair
  their code claim with a **visual** one — the Available suffix and closed-field name, hidden rows
  being _marked_, the Supplies cell list not being clipped — and reading code cannot settle those,
  so ticking them would have been a claim I had not earned. The spec's own change 5 already says
  what is left: _"Freeze still needs a Register spot-check and a 390×844 pass."_

  **What I did not do, and why:** drive it. The four surfaces are Move money and Assign — dialogs
  that write budget rows — and the only dev server running tonight is yours on 3047, against your
  real data. 09-10 built a `planner_overnight` database copy before clicking anything for exactly
  this reason. A ten-minute pass on those five spots is all that stands between this spec and
  **frozen / complete**; everything else in it is verified and green.

- **Nothing makes the cross-user suite grow.** `crossUserReads.integration.test.ts` names its 174
  functions by hand, so a new `queries.ts` export is covered only if somebody remembers to add
  it. Tonight's diff came back clean (above), but it came back clean by my reading it, not by
  anything in the gate. The shape that would fix it is the one 09-09 recommended for the settings
  registry: a test that enumerates every exported query function and asserts each is either in
  the suite or on a named opt-out list. I did not build it because the opt-out list is the
  decision — 15 functions are legitimately absent today, and which of those _should_ be exercised
  directly rather than through the loader they compose is a judgement about the suite's contract,
  not a mechanical fact. Cheap to add once that call is made.

## Gate status at end of run

_(filled in at the end)_
