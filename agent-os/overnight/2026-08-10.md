# Overnight run — 2026-08-10

Started 21:04. Focus: self-directed.

Baseline: typecheck ✅ at start; tree clean on `9d78d64`.

## Shipped

- `ce10c0a` — **Weekly-plan "last week's rewrite" date was one day early west of the
  server.** `loadWeeklyPlanPayload` serialized `previousRewrites[].weekStart` as
  `Date.toISOString()` of process-local midnight; GoalsStep formatted that with
  `toLocaleDateString` and no timezone. On UTC production a Sunday midnight Z becomes
  Saturday evening in America/New_York. Now ships a `YYYY-MM-DD` day key and formats with
  `formatDateKey(..., "MMM D")`; agent contract uses `dateKey` for the same field. —
  verified by `dateFormat.test.ts` ("keeps a plan-week day label…") + full unit +
  integration planning/agent green

- `6c6c874` — **Metrics CSV import broke on quoted multi-line cells** that
  `escapeCsvField` can write (and a hand-edited sheet can contain). `parseEntriesCsv`
  split on newlines then `splitCsvLine`, so one logical row became two errors. Switched to
  `parseCsvRows`. Added unit tests for `markers`, `csv/text`, and `notes/derive` — pure
  logic that was only reached through other suites. —
  verified by `parseEntriesCsv` multi-line case

- `b5704e2` — **Day-line settle/reopen/sync updates scoped only by `dailyItems.id`.**
  Rows were already loaded with `userId`, so this was defense in depth rather than a live
  leak; still matches the project rule that every mutation scopes by user. Also
  `distinct.test.ts` for the shared column-value walk. —
  verified by day/tree unit + integration green

- `f8e1aa7` — **Agent `search_notes` snippets left fence markers and table pipes.**
  `noteSearchSummary` used a naive character delete; the Notes grid already used
  `noteSnippet`. Shared the prose stripper so agent hits match the grid. —
  verified by `serialize.test.ts`

- `e43e31e` — **Agent get/create/update note reloaded the whole tree** to return one
  row. Pointed them at `loadNote` (same user scoping, one query). —
  verified by agent integration suite

- `d379579` — **`isTypingTarget` used `instanceof HTMLElement`**, which throws when the
  constructor is missing (Node unit tests). Duck-types `tagName` /
  `isContentEditable` instead; covers group-by picker helpers in `grouping.test.ts`. —
  verified by `keyboard.test.ts` + `grouping.test.ts`

## Tried and backed out

_(none)_

## Needs a decision

- Completing a project from the **detail drawer** vs from a **grid** still diverges:
  grids confirm and cascade-settle open descendants; the drawer calls
  `applyStateTransition` + upward-only `reopenSettledAncestors` and leaves children open.
  Prior overnight recommended adding `openDescendantCount` to the detail payload (option a).
  Still needs acceptance criteria as a small delta-spec.
  — blocking: consistent Complete on every surface

## Noticed but didn't touch

- `src/lib/agent/outlineTools.ts:147` — `get_node` still loads the full outline only to
  build the path label and a derived row. `loadNote` pattern for notes; a single-node
  path walk would be the outline counterpart. Not wrong, just heavy for large trees.
- `src/lib/agent/noteTools.ts` list/search still call `loadNotes` (full bodies) — needed
  for body search until a server-side body match exists like the Notes grid uses.
- Weekly planning `weekStart` storage still uses process-local midnight via `startOfWeek`
  rather than UTC-noon calendar days. GoalsStep display is fixed; Hawaii-edge `startOfWeek`
  on `fromDateKey` remains a latent geometry concern.
- Overview organizer acceptance still has an unchecked visual light/dark browser pass
  (`2026-08-09-2133` plan). Freeze is waiting on that, not on more code.
- Google event colours follow-ups: Day tab colour chips; agent `colorId` on
  create/update appointment — both need a small delta-spec.
- Roadmap residual: find-in-outline is day-to-day friction but UI placement is a product
  call; skip unattended.
- Pomodoro / GTD Someday-Maybe / invite UI remain morning work (design open).

## Gate status at end of run

| Check       | Result                                                                                        |
| ----------- | --------------------------------------------------------------------------------------------- |
| typecheck   | pass                                                                                          |
| lint        | pass (`--max-warnings=0`)                                                                     |
| unit        | pass — **148 files, 1770 tests**                                                              |
| integration | pass — **27 files, 509 tests** (Postgres up; no skip warnings)                                |
| build       | not re-run end-to-end (no routing/config changes tonight; hooks ran full suite on every push) |
| smoke       | not run (no UI chrome changes that need a live server; GoalsStep date is presentation-only)   |

Working tree clean; six commits on top of `9d78d64` pushed to `origin/master`.

## Run 21:44

Second pass (~20 min target). Focus: self-directed follow-ups from Run 1.

### In progress

_(closing run)_

### Shipped (this run)

- _(sha)_ — **`get_node` loaded the full outline** only to build a path label and depth.
  `loadNodeChain` walks ancestors (user-scoped, second-user null); format matches
  `buildPathMap`. Agent error classification unit tests.

- _(sha)_ — **Agent `parseDate("YYYY-MM-DD")` used UTC midnight** via `new Date(day)`,
  the encoding dates.md forbids. Bare day keys now go through `fromDateKey` (UTC noon);
  full ISO timestamps stay instants. Protects noteDate and any field stored as-is.
