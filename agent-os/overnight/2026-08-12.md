# Overnight run — 2026-08-12

Started 22:31. Focus: self-directed.

## Shipped

- `1f9bcb1` — **File evening Day appointments on the wall-clock day.** `/day` filtered
  `loadSchedule` occurrences with `toDateKey(startAt)`. That helper reads UTC date
  components and is correct for stored calendar days. A timed appointment is an instant:
  9pm Eastern is already tomorrow in UTC, so the pane filed it on the next day. Agenda
  already used `localDateKey`. Filter now lives in `src/lib/day/appointments.ts`.
  — verified by `appointmentsForDay` 9pm case, smoke, worktree `next build`, and the
  Day tab: Aug 12 showed `9 PM Overnight late-evening check`, Aug 13 showed nothing
  (`.artifacts/planner-shots/day-aug12-evening.png`).

- `31d2f84` — **Stop painting cancelled deadlines as overdue.** `DeadlineCell` treated
  only `completed` as finished (`state !== "completed"`), so a cancelled row with a past
  date stayed red. The same file already uses `isSettled` for the done checkbox;
  `scheduleStatus` already maps cancelled to completed. Quick capture's file-under list
  had the same leftover and would offer a cancelled project; the shared picker already
  excluded settled work.
  — verified by `isDeadlineOverdue` tests, picker settled-exclusion test, smoke, and
  Outline: open work `7/1/2026` is `text-priority-a`, cancelled is `text-ink-muted`.

- `dcf6ead` — **Walk derive inheritances through walkUp so a cycle cannot hang.**
  `walkUp` already exists so a corrupt parent cycle costs a truncated answer. `derive`
  still walked `parentId` by recursion for L.A.P., result-area name, project priority,
  category and shelf, and the effort rollup assumed every child had already been
  visited. A two-node cycle threw on the missing rollup bag and would have blown the
  stack on the inherited walks.
  — verified by `derive.test.ts` cycle + orphan cases plus the existing L.A.P. /
  category / rollup suite.

- `8dd33a0` — **Walk the project picker through walkUp so a cycle cannot hang.** The
  two ancestor loops that rebuild the visible tree had no seen-set. A corrupt parent
  cycle would spin forever while filling `shownIds` or rebasing depth.
  — verified by `picker.test.ts` cycle case plus the existing hierarchy/filter suite.

- `441640d` — **Ask owningProject and resultAreaName instead of walking again.**
  Tasks `__none__` scope walked `parentId` by hand (hangs on a cycle).
  `owningProjectId` already answers that. The schedule projects rail re-walked
  for Result Area grouping even though `derive` had written `resultAreaName`.
  — verified by typecheck, lint, smoke; `owningProjectId` already has the cycle case.

- `d20aa16` — **Load the Day tab's appointments for that day, not the week.**
  `dayRange` already existed for this tab. Loading a Sunday-aligned week and
  filtering worked, but it pulled six days the pane never shows.
  — verified by typecheck and smoke.

## Tried and backed out

_(none)_

## Needs a decision

- Completing a project from the **detail drawer** vs from a **grid** still diverges:
  grids confirm and cascade-settle open descendants; the drawer calls
  `applyStateTransition` + upward-only `reopenSettledAncestors` and leaves children
  open. Prior overnight recommended adding `openDescendantCount` to the detail payload.
  Still needs acceptance criteria as a small delta-spec.
  — blocking: consistent Complete on every surface

- **`blockDraft.ts` still compares range days with `toDateKey`.** Those `Date`s are
  local midnight (or UTC noon from `fromDateKey`), and in the pinned Eastern zone
  `toDateKey` and `localDateKey` agree, so I could not write a failing test. Switching
  to `localDateKey` is the dates.md-correct form but is a latent east-of-UTC issue,
  not a live Eastern bug.
  — blocking: nothing tonight; flip it when someone wants the dates.md cleanup

- **Finances insights dashboard** (`2026-08-12-2031`) is active with Layers 1–2
  (classify / transfer / income) shipped and the dashboard / analytics / reclassify
  mutation still unchecked. The remaining work is a multi-task feature, not a two-
  sentence overnight item.
  — blocking: the rest of that spec

## Noticed but didn't touch

- `src/lib/schedule/blockDraft.ts:30` — `toDateKey(entry) === todayKey` on local-
  midnight range days. See Needs a decision.
- Remaining parent walks: `notes/derive.ts` (forward pass, not recursive).
  `chooser/views.ts` already uses `walkUp`. `wishQueries.ts`, `dnd.ts`,
  `conversion.ts`, `notes/slice.ts`, and `completionCascade.ts` already carry a
  seen-set. I stopped converting more walk sites after five related commits.
- `src/lib/finances/grouping.ts` month-only grouping keys on `MM` with no year, so
  every August across years becomes one header. Year-then-month (the intended
  arrangement) is fine. Month-only is a product call.
- Roadmap residual: find-in-outline, Pomodoro, GTD Someday/Maybe, envelopes remain
  morning work (design open).
- Frozen schedule spec follow-up "`/day` files evening appointments on the wrong
  day" is now the shipped `1f9bcb1`. Do not edit that frozen folder.

## Gate status at end of run

| Check       | Result                                                                      |
| ----------- | --------------------------------------------------------------------------- |
| typecheck   | pass                                                                        |
| lint        | pass                                                                        |
| unit        | pass (2015)                                                                 |
| integration | pass on last push (2578 total) — Postgres was up; DB tests ran              |
| build       | pass (worktree, before first commit; later changes were not routing/config) |
| smoke       | pass (26/26) after Day page, capture action, and Tasks/rail edits           |

Tree clean on `d20aa16` once this report lands. Six work commits + report
commits pushed to `origin/master`.
