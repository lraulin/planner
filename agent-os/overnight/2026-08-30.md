# Overnight run — 2026-08-30

Started 20:53. Focus: self-directed.

## Shipped

- `83a7685` — Keep balanced parentheses in extracted URLs. `peelTrailingPunctuation` treated every trailing `)` as wrapping prose because the shorter form still normalized, so Wikipedia paths like `/wiki/Plan_(drawing)` lost the closer and attached the wrong href. Unbalanced `)` / `]` still peel. — verified by `taskNameLinks.test.ts` ("keeps a closing parenthesis that is part of the path", "peels a markdown-wrap closing bracket"); typecheck, lint, 3817 unit, `next build`.
- `be02b8e` — Pin the bank-snapshot suite to a live scrape hold. Fixture `scrapeBalanceAsOf` was `2026-08-29T12:00:00Z`; the 36-hour window expired on the evening of Aug 30, so pending scrape rows dropped out of envelope activity (`activityCents` 0). Apply then wrote `capturedAt` (still inside the window) and the same rows jumped back in (`-27663`). Capture time is now the test run. — verified by `bankSnapshotApply.integration.test.ts` (4 tests, Postgres up).
- `a5beae0` — Filter Activity Time by the wall-clock day. `occurredAt` is an instant; the Time column used `toISOString().slice(0, 10)` as the date-filter value, so a 9pm Eastern event shown as Aug 30 disappeared when the filter was Aug 30. Contacts / Resources / Time Charts already used `localDateKey` for `updatedAt`. — verified by `occurredDay.test.ts`.
- `b859c02` — Category picker lib variants for destinations: `includeCreate: false` drops empty types and New…, `allowClear: false` restores on empty, `detail` is not a filter token, `visibleEnvelopeCatalog` drops hidden envelopes and hidden-group descendants. — verified by `groupEnvelopeOptions.test.ts` (20 tests).
- `0aee761` — `CategorySelect` optional `onCreate` / `allowClear` / `placeholder` / `row.detail`. Register still passes `onCreate`.
- `9ec107b` — Move money To and Assign Manual To are the typeahead (no New…, cannot clear, hidden omitted; Available only on Move). — verified in the browser: Assign → Manually → To opens Budget tree (Regular spending / Bills / Savings), first envelope Discretionary, no New…, no Income options. Screenshots `.artifacts/planner-shots/assign-manual.png`, `assign-to-open.png`.
- `6f0caf7` — Payees learned/fixed Category is the typeahead over the full catalog (Income and hidden stay, placeholder None, no New…). Mode stays a select. — verified in the browser: payee drawer shows Learned Category as a combobox with placeholder None (`.artifacts/planner-shots/payee-drawer.png`).
- `0a0bea1` — Supplies Funded from is the typeahead; `listSupplyEnvelopes` deleted. Income dropped in the view; hidden stay; clearable to —. — verified the cell is a combobox (`Envelope funding Canned Cat Food`). The column sits far right; did not get a clean open-list screenshot.

## Tried and backed out

- First `git push` failed because Docker was not running (pre-push starts Postgres). Started Docker Desktop and retried.
- Second push failed on `bankSnapshotApply.integration.test.ts` — that was the 36-hour scrape-hold flake, not the URL change. Fixed the fixture rather than the product.

## Needs a decision

- **Per-user timezone (still open from 2026-08-07 / 2026-08-25).** `localDateKey(new Date())` is correct on this laptop (`TZ=America/New_York`) and wrong on Vercel (UTC) every evening ~20:00–midnight Eastern. Tonight's Activity Time filter is the same class, now consistent with Contacts / Resources, but production evenings still jump. Recommendation remains: store IANA zone on the account.
  — blocking: correct evening `/day` on the phone; Activity date filter on the deployed app; DST expansion of recurring Google appointments.
- Completing a project from the **detail drawer** vs from a **grid** still diverges (grids confirm and cascade-settle; drawer is upward-only).
  — blocking: consistent Complete on every surface.
- **Category picker everywhere** (`2026-08-30-1331`) is still **active**. Tasks 2–6 shipped. Freeze after a Register spot-check (create + hidden still present) and a 390×844 pass.
  — blocking: closing that spec.

## Noticed but didn't touch

- `src/lib/banksync/sync.ts:94` — `today` is `new Date().toISOString().slice(0, 10)` (UTC). Same evening trap as finance pages used to have. Fetch window one day long after 8pm Eastern; left alone because it is server-side "now" for a provider request, not a displayed calendar day.
- `src/lib/banksync/mapping.ts` `epochToDateKey` uses UTC date of SimpleFIN unix seconds. Tests pin midnight-UTC fixtures. Changing it to local needs a call on whether SimpleFIN `posted` is a calendar day or a real instant.
- `src/lib/finances/mutations.ts` `todayInUtc` is documented as an audit stamp, not a calculation.
- `src/lib/finances/billLastCharge.ts` has no sibling isolation test.
- Supplies Funded from sits off the right edge of the worksheet at desktop width; the portal should still place the list, but I did not prove it.

## Gate status at end of run

typecheck — pass (pre-commit on every commit; last explicit run after Supplies)  
lint — pass (pre-commit)  
unit — pass (3817 at first commit; picker tests 20/20)  
integration — pass when Postgres was up after Docker start; `bankSnapshotApply` 4/4 after the hold pin  
build — pass (`next build` before the first commit of the night)  
DB tests actually ran on push after Docker was started. Spec `2026-08-30-1331` left **active** (no freeze without Register + mobile pass).
