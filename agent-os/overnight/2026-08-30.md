# Overnight run — 2026-08-30

Started 20:53. Focus: self-directed.

## Shipped

- (pending) Keep balanced parentheses in extracted URLs — `peelTrailingPunctuation` treated every trailing `)` as wrapping prose because the shorter form still normalized, so Wikipedia paths like `/wiki/Plan_(drawing)` lost the closer and attached the wrong href. Unbalanced `)` / `]` still peel. — verified by `taskNameLinks.test.ts` ("keeps a closing parenthesis that is part of the path", "peels a markdown-wrap closing bracket")

## Tried and backed out

## Needs a decision

## Noticed but didn't touch

## Gate status at end of run

(in progress)
