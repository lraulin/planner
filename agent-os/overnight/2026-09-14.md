# Overnight run — 2026-09-14

Started 21:36. Focus: self-directed.

## Shipped

- `d13b0124` — Added `scripts/scrape-duplicate-report.ts` and the pure matcher
  `src/lib/finances/scrapeDuplicateReport.ts` (Task 7's second half from the active
  `2026-09-14-1004-ledger-ready-to-assign` spec): a read-only report of `scrape:*` posted
  rows on feed-covered accounts that line up with a feed row at the same amount and date —
  the duplicates D4 stopped creating but didn't remove (Sep 13 Walmart, Capital One payment
  pairs). Deliberately not description-gated like `feedPairing.ts`'s `pairRows`, since the
  duplicates it exists to find are exactly the ones whose page/feed descriptions share
  nothing (`Amazon.com` / `AMAZON MKTPL*537NK9DZ2`). Verified by
  `src/lib/finances/scrapeDuplicateReport.test.ts` (9 tests) and a real run against local
  dev data (`npx tsx --env-file=.env.local scripts/scrape-duplicate-report.ts --user
1015b266-dc43-41b7-9a3c-1b958ff1bbd2` → 0 matches, expected since the production incident
  data isn't in the dev seed).

## Tried and backed out

(none yet)

## Needs a decision

- **Task 7's other half — the Chase pending-sign parse fix (D6) — was not attempted.** The
  spec says to read the stored raw `amount` for `PAYMENT THANK YOU - WEB` from the Sep 14
  audit event's `source_evidence` before writing the fix. That event isn't in the local dev
  database (checked: `select * from finance_audit_events where source_evidence::text like
'%PAYMENT THANK YOU%'` → 0 rows) — it's production/Neon-only data, and this environment
  has no production DB credentials (`.env.local`'s Neon line is commented out with a
  placeholder). Guessing the exact non-ASCII minus character without seeing the raw text
  risks shipping a parse "fix" that doesn't address the real bug, in code that moves real
  money. **Recommendation:** next session with production access (or after Lee pastes the
  raw `source_evidence` JSON for that event), read the actual `PAYMENT THANK YOU - WEB` raw
  amount string, confirm which character the userscript's `AMOUNT` regex dropped, then fix
  `scripts/chase-pending.user.js`'s `AMOUNT` regex (or normalize the captured string) with a
  test on that literal raw text. — blocking: Task 7's acceptance criterion and Task 9
  (freeze).
- **Task 8 (cutover) and Task 9 (freeze/deploy verification)** were left alone — both
  explicitly need Lee: Task 8 wants a verified backup and Lee reviewing the dry-run receipt
  before apply; Task 9 wants Lee running both bank-page scripts and a sync against
  production after deploy. Correctly out of scope for tonight per the spec's own wording,
  not a fresh discovery.

## Noticed but didn't touch

(none yet)

## Gate status at end of run

typecheck / lint / unit / integration / build — all pass. Unit: 351 files, 4351 tests.
Integration: 66 files, 1102 tests, no skip warning (Postgres confirmed up,
`planner-postgres`, healthy). Build: clean.
