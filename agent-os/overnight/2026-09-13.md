# Overnight run — 2026-09-13

Started 20:36. Focus: self-directed.

Picked up from last night's "Noticed but didn't touch" list rather than re-sweeping ground
already covered (finance, cross-user scoping, dead exports, the spec D-lists).

## Shipped

- `4227d3a4` — **An IME composition's confirming Enter also fired the app's commit/submit.**
  Flagged last night as noticed-but-not-spread ("the only evidence it matters to you is one
  dev-database note with CJK text"). Typing in a CJK input method previews the syllable being
  composed; Enter confirms it into the field. None of the app's 37 `key === "Enter"` handlers
  checked for that, so the same Enter also ran the handler's commit/submit/select — on the
  value as it stood mid-composition, which drops or duplicates the last character depending on
  browser and timing. Added `isComposingKey` to `src/lib/keyboard.ts` (checks the standard
  `isComposing` flag and the legacy `keyCode === 229` some browsers send instead) and applied
  it at 17 genuine free-text commit sites: the two shared grid cell editors (row names used by
  nearly every grid; free-text cells used by Goals/Wishes), note titles and bodies, budget/bill
  category and chart names (three near-duplicate `RenameInput` copies — billColumns.tsx says
  outright it was "copied rather than imported" from Budget's), the contact discussion composer,
  quick capture, and Find's search box.
  Left alone on purpose: numeric and date fields (not composable), single-letter priority/rank
  inputs (`A1`, not realistic IME targets), menu/row Enter-or-Space activation (not text), and
  the combobox pickers (ContactSelect, ExercisePicker, TitlePicker, CategorySelect, the command
  palette) — same shape but wrapped in arrow-key list navigation that deserves its own look
  rather than a blind copy of this patch. Also skipped `src/components/day/**`
  ([[day-view-may-be-removed]] — Day view is on its way out).
  Verified `isComposingKey` with unit tests (including the plausible mistake of checking only
  `isComposing` and missing the 229 fallback), and the real browser mechanics on a throwaway
  `example.com` tab — no app, no database — with an actual `new KeyboardEvent("keydown", {
isComposing: true })` dispatch: suppressed as expected; an ordinary Enter still fires;
  `keyCode: 229` also suppressed.

- `ed62d601` — **The same IME bug, in the five combobox pickers.** Held these back from the
  first commit on purpose to give the surrounding arrow-key navigation its own look rather than
  copying the guard blind. Confirmed the same shape in all five: `ContactSelect.tsx`,
  `ExercisePicker.tsx`, `TitlePicker.tsx`, `CategorySelect.tsx`, and `CommandPaletteDialog.tsx`
  all fire `Enter -> choose(highlighted row)` while a filtered list is open from live-typed
  text, none checking `isComposing`. Confirming a composition there prematurely picks whatever
  the not-yet-final query happens to have highlighted — lower severity than the commit-on-Enter
  bug (it picks the wrong row rather than corrupting saved text), same missing guard. Applied
  `isComposingKey(event.nativeEvent)` before each `preventDefault`/`choose`, same as the first
  commit. Gate: typecheck, lint, unit (343/4266) and build all pass; no DB or routing touched,
  so integration/smoke weren't re-run for this cycle.

- `30b8dc5a` — **Test coverage for `extractHttpUrls.ts`**, which had none despite real logic:
  peeling trailing sentence punctuation, telling a URL's own balanced paren (Wikipedia-style
  paths) from an unbalanced one wrapping prose, a bare-host promotion guard that deliberately
  rejects "Untitled" and "v1.2", and `rewriteNameReplacingUrls`'s right-to-left index math plus
  its stale-index guard. Feeds real writers — clipboard attach, task name links — so a wrong
  answer here silently mangles a pasted link. 17 tests, all passing against the current
  implementation: this pins existing (correct) behavior, including the deliberate asymmetry
  that a bare IP host like `192.168.1.1` is rejected by the same version-number guard that
  rejects `v1.2`. Gate: typecheck, lint, unit (344/4283) pass; pure lib change, no DB or
  routing touched.

- `5bf3ac8d` — **Two `agent-os/standards/index.yml` entries had drifted from their files.**
  `migrations.md` grew a "Recovery gate" section (fresh verified backup + a Neon recovery
  point required before any destructive/data-transforming migration) that the index's
  one-liner never mentioned. `clean-code.md`'s "When the model is wrong, change the model"
  section — the one CLAUDE.md names and points agents at directly — was missing from its
  index summary entirely, so a narrow-context spec-shaping read of the index alone would
  never have surfaced it. Checked the other standards files touched this week
  (data-grid/drawer-pattern/navigation/responsive/ux-principles) and judged their commits
  path/wording corrections already covered by the existing one-liners — left those alone
  rather than pad the diff. Docs-only; no code gate needed.

- `9e734ecb` — **Test coverage for `oauth/origin.ts`**, which had none despite every route
  under `src/app/oauth` and `src/app/api/mcp` depending on it — and none of that `"use server"`
  surface is exercised by the unit suite at all (`testing.md`'s own named gap). Covered
  `publicOrigin`'s env-var precedence, trailing-slash strip, and request/default fallback
  chain, and `oauthSigningSecret`'s `BETTER_AUTH_SECRET`-over-`PLANNER_AGENT_API_KEY`
  precedence and its throw when neither is configured. Followed the existing convention in
  `oauth/authorize.test.ts` of setting `process.env` directly, but added save/restore
  (`beforeEach`/`afterEach`) since the unit suite runs non-isolated — an unrestored delete of
  `BETTER_AUTH_SECRET` would leak into whichever oauth test file the worker runs next. Full
  unit suite (345 files, 4294 tests) still green after adding it. Gate: typecheck, lint, unit
  pass; no DB/routing touched.

- **Spent a cycle on a fork hunting for one more concrete, verified bug** rather than more
  test coverage, since that's the higher tier when available. Result: **nothing solid.** It
  traced several promising-looking leads (a `convertNode` detail-table delete filtered only
  by `nodeId`, no `userId` in its own WHERE) to ground and found them already safe by an
  established pattern (`requireNode` proves ownership earlier in the same transaction, same
  shape as `subtreeIds()`). It also checked the roadmap's "a routine can never read as
  Overdue" claim against deadline+recurrence coexisting — confirmed current, correct,
  documented behavior (`recurrence/anchor.ts:24`, spec `2026-08-01-1900-recurrence-ap-parity`),
  the roadmap line is just imprecise, not worth its own cycle. Its conclusion, which matches
  what I was independently finding by hand before I forked it: this codebase's mutation/query
  layer has had so many overnight userId-scoping and derived-state passes already that further
  bug-hunting is lower-yield than test coverage right now. Spent no commit on this, just the
  research.

- `dc0ca929` — **Test coverage for the Insights report-settings parser.** `parseReportSettings`
  had no test despite real branching: an `accountIds`/legacy-`accounts` fallback, a
  `categoryIds`/`payeeIds`-versus-migrate-from-legacy-names fallback, and
  `migrationWarnings` assembled from both migrations' unresolved names. The sharp edge: an
  explicitly empty `categoryIds` array must win over re-running the name migration — it means
  "no categories selected," not "not yet migrated" — easy to get backwards with a plain `??`
  instead of `asStringArray`'s is-array check. `migrateReportNames` and the `settings/parse.ts`
  primitives it composes are already tested elsewhere; this covers only this function's own
  wiring. 8 tests, all passing against current behavior. Gate: typecheck, lint, unit
  (346/4302) pass.

- `0a24e96b` — **Test coverage for `asyncHandler`.** Its own docstring explains why it exists
  (an async function passed straight to `onClick` type-checks but React drops the rejection on
  the floor) yet nothing proved it actually does the catch-and-report it promises. Used across
  7 components, zero coverage. 3 tests: a resolved call never touches `onError`, a rejected
  call routes to it with the fixed message, and the returned handler is synchronously void
  rather than leaking the wrapped promise. Gate: typecheck, lint, unit (347/4305) pass.

## Tried and backed out

(none yet)

## Needs a decision

(none yet)

## Noticed but didn't touch

(carried forward from last night unless resolved above — see `agent-os/overnight/2026-09-12.md`)

## Gate status — checkpoint at 21:27

Full gate re-run against `0a24e96b`, after 8 commits (2 IME composition-bug fixes across 22
components, 4 test-coverage additions, 1 standards-index fix, 1 research fork that found no
further bug to hand off).

| Gate                       | Result                                                                                   |
| -------------------------- | ---------------------------------------------------------------------------------------- |
| `npm run typecheck`        | pass                                                                                     |
| `npm run lint`             | pass (`--max-warnings=0`)                                                                |
| `npm run test:unit`        | pass — 347 files, 4305 tests (4266 at the start of the night)                            |
| `npm run test:integration` | pass — 63 files, 1085 tests — Postgres up the whole night, ran every time, never skipped |
| `npm run build`            | pass                                                                                     |
| `npm run smoke`            | pass — all 62 routes rendered                                                            |

Every commit tonight was pushed individually and the tree is clean at this checkpoint. Run
continuing — more cycles to follow if the night has room for them.
